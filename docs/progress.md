# Sistema de Incorporación de Escuelas (SEDEQ) — Progress Ledger

**Spec:** `docs/PRD_Sistema_Incorporacion_MVP.md`, `docs/COMPENDIO_MAESTRO_Sistema_Incorporacion.md`, `docs/ddl_sistema_incorporacion_v3.sql`

**Stack:** Laravel 13 (PHP 8.4) · Livewire 3 · Filament 4 · PostgreSQL 18 · `spatie/laravel-permission` · `barryvdh/laravel-dompdf` · Laravel Breeze
(PRD specified Laravel 11 / Livewire 3 / Filament 3 / PostgreSQL 16 — bumped to current stable versions where the originals weren't installable together; see Decisions log.)

Format: checkbox per completed/pending unit of work. Check off as work lands; don't rewrite history — append.

---

## Phase 1 — Environment Setup

- [x] PHP 8.4.25 + Composer 2.10.2 via Laravel Herd
- [x] Node v24.19.0 / npm 11.17.0 / git 2.55.0 (pre-existing)
- [x] PostgreSQL 18.6 (pre-existing, added to user PATH)
- [x] DB role `sedeq_app` + database `sedeq_incorporacion` created, connection verified
- [x] Laravel project scaffolded (`app-laravel/`), Livewire, Breeze, Filament panel, Spatie permission, DomPDF installed
- [x] `.env` wired to PostgreSQL, `php artisan serve` confirmed working (`/`, `/login`, `/admin` all 200)
- [x] Repo restructured to monorepo: git root at `sedeq-incorporacion/`, `app-laravel/` (code) + `docs/` (reference docs) as siblings so docs travel with the repo on push
- [x] `git init` at root — **not committed yet**, waiting on remote repo access

## Phase 2 — Code Structure Scaffolding

- [x] `app/Livewire/Tramite/` — 8 wizard step components generated (`Paso1Preregistro`, `Paso2Responsable`, `Paso3/{Inmueble,Infraestructura,Mobiliario,PlanEstudios,PlantillaDocente,Matricula}`) — empty stubs, no logic yet
- [x] `app/Domain/Validacion/ValidacionCapacidadService.php` — Motor de Validación, isolated framework-agnostic per architecture review (see Decisions log) — empty stub
- [x] `app/Services/{ChecklistDocumentalService,FormatoSolicitudPdfService}.php` — empty stubs
- [x] `app/Enums/`, `app/Filament/Resources/` — empty placeholder dirs, blocked on models/catalog data existing first

## Phase 3 — Database Schema

- [x] All 44 tables from `ddl_sistema_incorporacion_v3.sql` converted to Laravel migrations (`app-laravel/database/migrations/2026_01_01_*`), one per table, in FK-dependency order matching the DDL. Each migration runs the exact `CREATE TABLE` SQL verbatim (`DB::unprepared`) to preserve every `CHECK` constraint and default with zero translation drift.
- [x] Migrated against PostgreSQL, verified via `\dt` — 44 domain tables + Laravel/Breeze/Spatie base tables, all present
- [x] FK-index migration (`2026_01_02_000000_add_indexes_to_incorporacion_tables.php`) — added indexes on FK columns flagged by `ecc:database-reviewer` review; run against dev DB
- [x] Minimal catalog seeds from DDL's "SEEDS MÍNIMOS" section (`niveles_educativos`, `estados_expediente`, `salas`, `tipos_material_biblioteca`) — `database/seeders/CatalogoMinimoSeeder.php`, idempotent (`insertOrIgnore`), wired into `DatabaseSeeder`, TDD-covered by `tests/Feature/Database/CatalogoMinimoSeederTest.php`
- [x] Partial catalog seed data unblocked by the 2026-09-04 COMPENDIO update: `asignaturas` (16), `cargos_puestos` (16, all 4 niveles), `perfiles_profesionales` (87 — Inicial/Preescolar/Primaria plus Secundaria Director Técnico; Secundaria docente-por-asignatura catalog still blocked, lives in external PDF), `reglas_validacion` (28, superficie+personal ratios across all 4 niveles). See `docs/superpowers/plans/2026-09-04-catalogos-motor-validacion.md` for full row-level detail, subagent-driven execution ledger, and deferred scope.
- [ ] Still not started: `tipos_documentos` (needs prose-to-table transcription of §4 checklist), `niveles_tipos_espacios`/`tipos_espacios`, `mobiliario_conceptos` beyond Educación Inicial (blocked, §7 pendientes), granular `reglas_validacion` detail (door/stair/corridor dimensions, ranged sanitation tables — deliberately deferred, not yet consumed by any Motor de Validación code)
- [ ] Lower-priority FK columns not yet indexed: `personal_asignaturas`, `personal_salas`, `matricula_semestres`, `matricula_cuatrimestres`, `aulas_nivel` — out of scope of the FK-index migration above, only reviewer-flagged columns were covered

## Phase 4 — Not Started

- [x] Eloquent models (25 exist; ticked 2026-09-23 — not one per table, only what the wizard needs)
- [ ] Livewire wizard logic (Paso 1-3, per PRD §5)
- [ ] Motor de Validación implementation (superficie/personal/mobiliario rules)
- [ ] Filament Resources for SEDEQ panel (expediente listing, detail view, estado transitions) — PRD §6
- [ ] PHP Enums for `estado_expediente`, `tipo_persona`, `tipo_tramite`, etc.
- [x] Formato de Solicitud PDF generation (dompdf) — exists; to be rebuilt to the official structure in the 2026-09-23 audit remediation (WS-5)
- [x] Roles/permissions setup — `RolesSeeder` + `sedeq` role gating /admin (2026-09-09)

---

## Decisions Log

Non-obvious calls made during setup that deviated from the PRD/doc as written — kept here so future work (and future-you) knows *why*.

- **Filament v4 instead of v3**: PRD's stack table said Filament 3, but `composer create-project laravel/laravel` pulled Laravel 13 (current stable), which resolves to Symfony 8 — incompatible with Filament 3's Symfony 6/7 requirement. User confirmed v4 over pinning Laravel back to v11. Filament v4 API is close enough to v3 that PRD's functional requirements (§6) aren't expected to need rework.
- **Livewire v4 instead of v3**: same cause — latest stable pulled by Composer, not pinned. Not yet verified against any Livewire-3-specific PRD assumptions (there don't appear to be any — PRD only references Livewire at the stack-choice level).
- **PostgreSQL 18 instead of 16**: was already installed on the machine; newer minor-compatible version, no reason to downgrade.
- **Monorepo, not nested-single-folder**: originally `git init` ran inside `app-laravel/`, leaving `docs/` outside version control. Moved git root to `sedeq-incorporacion/` so `docs/` (PRD, compendio, DDL — required "in the repository" per PRD §9) travels with the code.
- **Single Laravel app, not split repos/services**: considered and rejected splitting the Filament admin panel or the domain/validation layer into a separate package or repo. Independent architecture review (agents-development-architecture:backend-architect) confirmed: no team to isolate, no independent deploy/scaling need, DB schema as shared contract across repos would double migration overhead for one dev on a deadline. Recommended (and applied) tightening code-layer boundaries instead — `app/Domain/` for framework-agnostic business rules (the Motor de Validación is the concrete first case, since it's the piece Etapa 2/3 reuses across trámites), `app/Livewire`/`app/Filament` stay thin and call into it.
- **Migrations generated from raw DDL, not hand-translated Schema Builder**: 44 tables with many `CHECK` constraints Laravel's Schema Builder doesn't cleanly express. Each migration wraps the DDL's exact `CREATE TABLE` statement via `DB::unprepared()` instead — guarantees the schema matches the already-3NF-normalized, already-validated DDL exactly, per PRD §4 ("No rediseñar el esquema").
- **Tables stay in `public` schema, not a dedicated one**: considered and rejected. A custom schema earns its keep for multi-tenancy or isolating a second system sharing the DB cluster — neither applies now (single DB, single role, single tenant). If a future Etapa 2/3 piece (RecaudaNet integration, a reporting/analytics layer) ends up as a separate service on the same Postgres instance, schema that piece then, not the core tables preemptively.
- **Test DB switched from sqlite in-memory to a dedicated Postgres DB (`sedeq_incorporacion_testing`)**: `phpunit.xml` originally pointed tests at sqlite `:memory:`, per Laravel default. But the 44 domain-table migrations run raw Postgres DDL verbatim (`SMALLSERIAL`, etc., via `DB::unprepared()` — see the migrations decision above) to preserve `CHECK` constraints exactly; sqlite can't parse that DDL, so `RefreshDatabase` failed on the first domain-table migration for any test touching those tables. Fix: added `.env.testing` + updated `phpunit.xml`'s `<php>` block to use `pgsql` against a new dedicated `sedeq_incorporacion_testing` database (separate from dev `sedeq_incorporacion`, so tests never touch dev data). Verified: full suite (31 tests) passes against it. Tradeoff accepted: test runs need a reachable Postgres instance now, no longer fully self-contained — but the alternative (rewriting migrations to be sqlite-compatible) would have broken the "byte-for-byte DDL match" guarantee that's the whole point of the `DB::unprepared()` approach.
- **Seeder idempotency without schema changes, even when a table has no UNIQUE constraint**: `reglas_validacion` and `perfiles_profesionales` have no UNIQUE constraint in the source DDL, so `insertOrIgnore` alone doesn't prevent duplicates on a second run. An implementer subagent's first attempt at `perfiles_profesionales` fixed this by adding two partial unique indexes to that table's migration — rejected on review: migrations under `database/migrations/2026_01_01_*` are byte-for-byte DDL ports and must never be edited to work around a seeder problem (same principle as the migrations Decisions Log entry above). Both seeders instead use delete-then-insert scoped to what they're about to re-insert (by `nivel_educativo_id` for `reglas_validacion`, by `cargo_puesto_id` for `perfiles_profesionales`) — no schema change, verified zero drift from `ddl_sistema_incorporacion_v3.sql`.
- **Educación Física / trabajador social / prefecto staffing threshold set to "más de 60" (61), not "≥60" (60)**: COMPENDIO_MAESTRO is internally inconsistent — §5.1 says "≥60 alumnos"/"60 o más", §5.2 (the Acuerdos Secretariales text) says "más de 60". User confirmed 61 explicitly (2026-09-04) when raised during code review; §5.2's Acuerdos text is the more authoritative/later source. Documented inline in `ReglasValidacionSeeder`'s docblock so a future reader doesn't "fix" it back to 60 without new instruction.
- **`perfiles_profesionales` includes Secundaria's Director Técnico (7 profiles), but not the per-asignatura docente catalog**: the original plan excluded all of Secundaria from this seeder, reasoning the carrera catalog "vive en el PDF fuente." A whole-branch code review caught that this is only true for the per-asignatura docente catalog (hundreds of entries, genuinely not reproduced in COMPENDIO) — Director Técnico's 7 accepted profiles ARE fully specified in §5.1 line ~495. User confirmed (2026-09-04): add the 7 rows now (Educación Básica, Educación Media, Intervención Educativa, Pedagogía, Psicología Educativa, Ciencias de la Educación, Maestría en Educación, all `titulo_cedula`), raising the seeder's total from 80 to 87 rows.
- **`reglas_validacion` gained a `redondeo` column and `infraestructura` tipo_regla (2026-09-07, follow-up review)**: two defects found in the redesign below. (1) `tipo_calculo = personal_proporcional` was used for both Inicial's asistente ratios (COMPENDIO línea 422: "redondeo hacia arriba" — explicit) and Primaria's Educación Física rule (rounds down, per the floor decision below) with nothing in the schema distinguishing them — a naive engine implementation could not satisfy both, and using floor for Inicial would understaff a Lactantes room by half. Fixed by adding `redondeo VARCHAR(10) NOT NULL CHECK (redondeo IN ('arriba','abajo','na'))`; `'na'` for every non-`personal_proporcional` row (confirmed none of them divide anything, so none needed a rounding rule). (2) Three rows were misclassified under `tipo_regla = 'superficie'` despite measuring linear metres or book counts, not area (`primaria.superficie.altura_aulas`, `primaria.superficie.acervo_bibliografico`, `secundaria.superficie.acervo_bibliografico`) — added `'infraestructura'` to the `tipo_regla` CHECK and reclassified these three (new distribution: 16 superficie / 9 personal / 3 infraestructura = 28). Also fixed in the same pass: Primaria's PE rule previously combined `personal_proporcional` with a `condicion_min = 61` gate, producing a 0-to-2-teacher jump between 60 and 61 students — removed the gate, threshold now implicit via floor division. Created `app/Domain/Validaciones/Regla/CalculadoraRequerimiento.php`, the first real Motor de Validación code (pure, no DB), and rewired the seeder's boundary tests to exercise it instead of duplicating arithmetic inline. Full detail: `docs/reports/2026-09-07-reglas-validacion-schema.md`. The 60-vs-61 threshold question itself remains open — see `docs/decisions/PENDIENTE-umbral-educacion-fisica.md`, not resolved by this entry.
- **`reglas_validacion` schema redesigned (2026-09-07) before any engine code consumed it**: three defects fixed while the table still carried no production data. (1) Added `clave VARCHAR(100) UNIQUE NOT NULL` — a stable machine key (`{nivel}.{tipo_regla}.{slug}`) so the future Motor de Validación never has to switch on the free-text, accented `concepto` string. (2) Added `tipo_calculo` (9-value CHECK: `ratio_por_alumno`, `ratio_por_grado`, `minimo_fijo`, `adicional_fijo`, `factor`, `personal_obligatorio`, `personal_umbral`, `personal_proporcional`, `personal_por_espacio`) so the engine dispatches on an explicit calculation strategy instead of parsing `unidad` strings like `'trabajador social'`. (3) Added `ambito` (`aula`/`sala`/`plantel`/`escuela`/`predio`) disambiguating what `condicion_min`/`condicion_max` actually count — previously the same columns meant "aula capacity" for Inicial and "school enrollment" for Preescolar with nothing in the row saying which. `concepto` and `unidad` are now display-only. Also added `cargo_puesto_id` (nullable FK to `cargos_puestos`, left NULL — populating it is blocked on `CargosPuestosSeeder`'s data, not its schema). Migration (`2026_01_01_000010_create_reglas_validacion_table.php`) and `docs/ddl_sistema_incorporacion_v3.sql` edited in place (schema never deployed, no data to preserve). Two normative fixes surfaced during the redesign and confirmed against COMPENDIO §5.2 (Acuerdos Secretariales text, authoritative per the existing 2026-09-04 threshold decision): the Educación Física `condicion_min = 61` (>60) was re-verified as correct, not an off-by-one — §5.1's "60 alumnos o más" phrasing for Preescolar is the outlier COMPENDIO text, already superseded by the earlier decision. And Preescolar vs. Primaria Educación Física, previously seeded identically, are now split: Preescolar is `personal_umbral` (fixed 1 teacher once enrollment ≥61), Primaria is `personal_proporcional` (`ceil(enrollment / 60)` teachers, per §5.1's explicit "por cada 60... se puede requerir más de uno"). Seeder switched from delete-then-insert-per-nivel to `insertOrIgnore`, now safe since `clave` is UNIQUE. Full detail, the 28-row table, and taxonomy-fit notes: `docs/reports/2026-09-07-reglas-validacion-schema.md`. Follow-up: populate `cargo_puesto_id` once `CargosPuestosSeeder`'s Task 2 data exists in a form queryable by this seeder (currently no row links a `reglas_validacion` personal rule to a specific `cargos_puestos` row). **SUPERSEDED by the 2026-09-07 follow-up entry above: this entry's `ceil(enrollment / 60)` was a gate-plus-division defect (0-to-2-teacher jump at the 60/61 boundary) — the corrected formula is `floor`, not `ceil`. Do not follow the `ceil` guidance in this entry.**
- **`personal` columns made nullable for wizard draft rows (2026-09-07, branch `feat/wizard-progreso`)**: `cargo_puesto_id`/`nombre`/`nacionalidad`/`sexo`/`estudios`/`cedula_o_documento` dropped from `NOT NULL` (`escuela_nivel_id` stays `NOT NULL`) — explicitly authorized (not an ad hoc schema change) to support a resumable wizard where an applicant can save a half-filled Paso 3.5 (Plantilla docente) row and return later. This reopens the same hazard the `reglas_validacion` redesign was careful about: a table permissive enough to hold drafts must not let a draft silently look like real data to the Motor de Validación. Closed by `app/Domain/Personal/RegistroPersonalCompleto::esCompleto()` — a pure six-argument completeness check (all six non-null, and for the five string fields, non-empty-string) that the Motor's future personal-counting code **must** call before counting any `personal` row toward a staffing ratio; nothing calls it yet, since the counting logic itself is out of scope. Two new tables added in the same task, unrelated schema-wise but same authorization: `pasos_captura` (catalog of Paso 3's six sub-steps) and `escuela_nivel_pasos` (per-expediente sub-step progress, no code creates/advances rows yet). Full detail: `docs/reports/2026-09-07-wizard-progreso.md`. Follow-up left open: `escuela_nivel_pasos` has no per-nivel step-applicability model, so `mobiliario` (Educación Inicial only, per PRD §8) will get a row for every nivel unless whatever populates this table filters it — not decided.
- **ADR-001 (2026-09-07): the API-barrier's real-HTTP-round-trip design (D2) is dead, replaced with an in-process `app/Application/` layer**: the 2026-09-04 Opus review's finding (Livewire calling its own API by real HTTP can't work — no cookie jar server-side, `php artisan serve` is single-process, self-call deadlocks) is now a formal decision, not just an open finding. `docs/decisions/ADR-001-frontera-de-capas.md`: Livewire and the API controllers both call the same `app/Application/` use cases; the HTTP API is still built (versioned, OpenAPI via Scramble, contract-tested) but as a thin adapter for Etapa 3 external consumers, off the wizard's own path; boundary enforcement (Deptrac) deferred to after the MVP presentation, since static analysis over empty stubs proves nothing; the system is confirmed server-rendered (one route per wizard step, `redirect()` between steps), not a SPA. `docs/superpowers/specs/2026-09-04-api-layer-design.md`'s D2 and D3 annotated as superseded in place, original reasoning kept. No `app/Application/` code exists yet — this ADR sets the rule, doesn't implement it.
- **Motor de Validación: MVP scope and timing clarified directly with the user (2026-09-11), nothing built yet**: two points diverge from how COMPENDIO_MAESTRO §6 frames them, worth recording before lost. (1) Scope: automated pre-validation via the Motor (accept/reject with per-rule messages, e.g. "at least 2m² of aula construction per student") IS in MVP scope; human SEDEQ review (Etapa 2's actual reviewer acting on `estado_validacion`, notifications, the review workflow itself) is explicitly OUT of MVP scope. COMPENDIO §6 bundles both under one phase ("Etapa 2 — Validación y revisión interna") — this splits them: the Motor's evaluation belongs to the solicitante-facing MVP flow, the human layer on top of it does not. (2) Timing, corrected after an initial wrong assumption: the Motor does NOT run per-step or in real time as each field is captured — it runs ONCE, as a final batch evaluation after all of Paso 3's capture (Anexo 1 personal + Anexo 2 instalaciones + matrícula) is complete, confirmed directly by the user. This is distinct from `ValidarVigenciaDocumentos` (already built, Paso 2.2), which DOES gate per-step at the 2.2→2.3 transition — the two validators are separate mechanisms at separate points in the flow, not the same thing. Implication for Paso 3's eventual design, flagged now so it isn't lost: because the Motor needs the full Paso 3 dataset at once (compares superficie/matrícula/personal together, not rule-by-rule against partial data), Paso 3 needs a final submission/evaluation gate, structurally different from Paso 2's incremental per-phase `mount()`-resume-by-phase gates — that pattern won't directly apply to Paso 3's closing step. `docs/decisions/PENDIENTE-origen-de-magnitud.md`'s scope now explicitly includes not just where each rule's `magnitud` input comes from, but also at what point in the flow all of them get read together — that file's own content is not yet updated to reflect this, only noted here.

---

## Session Log

### 2026-09-03
- Environment setup completed end to end (Phase 1).
- Code structure scaffolded (Phase 2): Livewire wizard stubs, `app/Domain/Validacion`, `app/Services`, placeholder `app/Enums` and `app/Filament/Resources`.
- Repo restructured to monorepo (git root moved from `app-laravel/` to `sedeq-incorporacion/`) after catching that `docs/` was outside version control.
- Independent architecture review (backend-architect subagent) on monolith-vs-split-repo question — confirmed single Laravel app, recommended `app/Domain/` boundary; applied by moving `ValidacionCapacidadService` there.
- All 44 DDL tables converted to migrations and run against PostgreSQL (Phase 3, schema only — seeds still pending).
- This ledger (`docs/progress.md`) created.
- Discussed and rejected splitting tables into a dedicated Postgres schema — staying on `public`.

### 2026-09-04
- `ecc:laravel-patterns` and `ecc:database-reviewer` review agents run against current code/schema; database review flagged missing indexes on FK columns.
- FK-index migration (`2026_01_02_000000_add_indexes_to_incorporacion_tables.php`) created and run against dev DB for the flagged columns.
- Minimal catalog seeds (Phase 3) implemented: `CatalogoMinimoSeeder`, TDD (red→green), wired into `DatabaseSeeder`.
- Discovered and fixed a pre-existing test-DB mismatch: `phpunit.xml` forced sqlite in-memory, incompatible with the raw-Postgres-DDL domain migrations — blocked any `RefreshDatabase` test touching a domain table. Switched to a dedicated `sedeq_incorporacion_testing` Postgres DB (see Decisions log). Full suite (31 tests) verified passing against it.
- `wake-resume.sh` UserPromptSubmit hook (personalized `/ecc:resume-session` trigger phrase) fixed — `grep -oP` PCRE lookbehind wasn't supported by this environment's grep, causing a silent hook failure on every prompt; replaced with a portable `sed` extraction.
- COMPENDIO_MAESTRO updated by user: §5.2 added, full text of Acuerdos Secretariales 357 (Preescolar)/254 (Primaria)/255 (Secundaria) obtained from official SEP sources — closes the "Pendiente: Preescolar, Primaria y Secundaria (ratios de m²/alumno)" gap noted in §5. Motor de Validación superficie+personal rules now documented for all 4 niveles of Educación Básica.
- Written implementation plan created (`docs/superpowers/plans/2026-09-04-catalogos-motor-validacion.md`, via `superpowers:writing-plans`) for seeding `asignaturas` (16 rows), `cargos_puestos` (16 rows), `perfiles_profesionales` (80 rows, Inicial/Preescolar/Primaria only — Secundaria carrera catalog still blocked, lives in external PDF), `reglas_validacion` (28 rows, superficie+personal core ratios across all 4 niveles — granular door/stair/corridor/sanitation-table detail deliberately deferred). **Not yet executed** — next step is subagent-driven execution (`superpowers:subagent-driven-development`), task by task starting with `AsignaturasSeeder`.
- Git repo hygiene fixes: `user.name` was never set globally (only a typo'd `user.email`, `outllok.com`) — root cause of the original "can't resolve user" commit error. Fixed global config (`Emiliano Ledesma <emilianoledesmaledesma@outlook.com>`, confirmed against `gh auth status` → account `EmilianoLedesma`), then amended the repo's single existing commit's author and force-pushed (`cec448f` → `dbb5fe6`). Repo confirmed public (`EmilianoLedesma/Expendiente_Escuelas`).
- Per user request: removed `docs/ddl_sistema_incorporacion_v3.sql` and `docs/INSTRUCCIONES_SETUP_ENTORNO.md` from git history entirely (`git rm --cached` + amend + force-push, since repo has only ever had one commit — no filter-repo needed). Both files remain on local disk, untracked; `.gitignore` now has `*.sql` and an explicit `docs/INSTRUCCIONES_SETUP_ENTORNO.md` line. Commits: `dbb5fe6` → `ffad2c0` (DDL removed) → `662f5a6` (INSTRUCCIONES removed). **Note for future sessions:** this reverses the 2026-09-03 Decisions Log entry "Monorepo, not nested-single-folder" reasoning that `docs/` should travel with the code per PRD §9 — DDL/setup-instructions are now local-only by explicit user choice, not a random omission.
- README.md updated to reflect the above: `docs/` section and Setup section now note the DDL/instrucciones files are local, not versioned. **Not yet committed/pushed** — was in progress when session paused (`.gitignore` + `README.md` unstaged changes present).
- **Catalog-seed plan executed** (`docs/superpowers/plans/2026-09-04-catalogos-motor-validacion.md`) via `superpowers:subagent-driven-development` in an isolated worktree (`.claude/worktrees/catalogos-motor-validacion`, branch `worktree-catalogos-motor-validacion`). All 5 tasks (`AsignaturasSeeder`, `CargosPuestosSeeder`, `PerfilesProfesionalesSeeder`, `ReglasValidacionSeeder`, `DatabaseSeeder` wiring) implemented, individually reviewed, and merged to `master` (commit `6eb8ccb`, merge via `git merge --no-edit`). One fix round mid-plan (Task 3's migration-scope violation, see Decisions log). A final whole-branch review (opus) caught 3 Important findings: 1 real code bug (fixed — see Decisions log, delete-scope), 2 business-rule ambiguities routed to the user rather than guessed (60 vs 61 threshold, Secundaria Director Técnico scope — both resolved, see Decisions log). 4 Minor findings parked as deferred, non-blocking: Preescolar Docente de Inglés `titulo_cedula` vs. compendio's `certificado` labeling (judgment call, arguably correct per Primaria's cross-reference), no end-to-end `DatabaseSeeder` ordering test, a seeder variable named like a scalar but holding a map, no test asserting `reglas_validacion.fuente` is populated. Full suite: 49/49 passing (31 pre-existing + 18 new). Worktree and feature branch deleted after merge. Merge pushed to remote (`da7aebd`).
- `wake-resume.sh` updated: the wake-phrase hook now also instructs the resuming session to read `docs/progress.md` in full alongside the saved session file, with `progress.md` treated as authoritative if the two disagree (per user request — keep both up to date and in sync).
- **Dev database seeded** (`php artisan db:seed` against `sedeq_incorporacion`, not the test DB — user explicitly confirmed this write first, per CLAUDE.md data-safety rule). Confirmed via tinker row counts: `niveles_educativos` 7, `asignaturas` 16, `cargos_puestos` 16, `perfiles_profesionales` 87, `reglas_validacion` 28. Dev DB had been completely empty until this point — all seeding up to now had only ever touched the dedicated `sedeq_incorporacion_testing` DB via the test suite.
- **API separation-layer brainstorming session** (`superpowers:brainstorming`, architectural path): decided the solicitante-facing Livewire wizard must talk to the backend only through a real, versioned HTTP JSON API (`/api/v1/...`) — never touch Eloquent/Domain services directly — while Filament (SEDEQ admin panel) stays exempt for reads (Eloquent-driven Resources). Researched `EmilianoLedesma/SWAY` (user's own separate FastAPI project) via `gh api`/`gh repo view` (not cloned locally) as a conventions reference: routers/Pydantic-schemas/SQLAlchemy-models/security/services split, generated-not-hand-written OpenAPI, one central per-consumer API client file with normalized error handling. User then set this as a **standing rule for the whole project**, not just this feature — saved to long-term memory (`feedback_api_separation_layer.md`). Design spec written to `docs/superpowers/specs/2026-09-04-api-layer-design.md` (contract-first: versioned routes, OpenAPI via Scramble, async Motor de Validación job on the database queue driver, Sanctum dual-mode auth — cookie for Livewire, tokens for external clients — client-wrapper class per resource so Livewire never calls `Http::` directly).
- **Opus review of the spec** (background subagent, model `opus`, read spec + PRD + COMPENDIO §3/§5/§5.x/§7 + this file + CLAUDE.md + actual repo state) returned **3 blocking findings**: (1) the spec's "real HTTP round-trip, cookie-mode auth" design for Livewire's internal calls doesn't actually run — the client wrapper is server-side PHP issuing `Http::`, so there's no browser cookie jar for Sanctum's SPA/cookie mode, and `php artisan serve` is single-process, so a self-call deadlocks; likely fix is moving the actual HTTP call into the browser (JS/Alpine) instead of PHP-to-PHP. (2) The async validation-job design has nowhere to persist run state — no table for it exists in the 44-table DDL, and CLAUDE.md forbids inventing schema outside the DDL/PRD source of truth. (3) No draft/partial-save model was defined for the wizard's per-step "guardar y continuar," despite the verbatim-DDL migrations' NOT NULL/CHECK constraints making a half-filled step unable to insert as-is. Plus 4 non-blocking gaps: async validation likely contradicts COMPENDIO §5's "tiempo real" expectation and is cheap enough to run synchronously; Filament's write path (not just reads) needs to route through the same Domain services the API uses, or it silently bypasses validation on shared tables; file/document upload flow (PRD §5 Paso 2.2) isn't designed; catalog-dropdown reads (tipos_documentos, cargos_puestos, etc.) aren't addressed. Also surfaced: `laravel/sanctum` isn't in `composer.json` yet, `routes/api.php` doesn't exist, and `composer.json` pins `livewire/livewire ^3.6.4` — contradicting this file's and CLAUDE.md's claim of Livewire 4 (a separate doc-accuracy issue, not part of the spec). **Verdict: spec needs a revision pass before moving to an implementation plan.**
- User flagged (2026-09-04, end of session): `PRD_Sistema_Incorporacion_MVP.md`, `COMPENDIO_MAESTRO_Sistema_Incorporacion.md`, and `CLAUDE.md` may all have been written **before** the API-separation-layer decision existed, so passages assuming direct Livewire→Eloquent access or synchronous-only flows may need reconciling against the new architectural rule — not yet scoped as its own pass beyond what the Opus review already surfaced (the COMPENDIO §5 "tiempo real" point above). **Not yet resolved — next session should pick this up before revising the spec further.**

### 2026-09-07
- **`reglas_validacion` schema redesign** on branch `arch/reglas-validacion-schema`, four review rounds, each catching a defect the prior round missed — full detail in `docs/reports/2026-09-07-reglas-validacion-schema.md`, decisions summarized in this file's Decisions Log above. Round 1: added `clave`/`tipo_calculo`/`ambito`, split Preescolar (`personal_umbral`) from Primaria (`personal_proporcional`) Educación Física. Round 2 (follow-up): fixed a gate-plus-division discontinuity in Primaria's rule (60→0, 61→jumps to 1-2 teachers); reopened the 60-vs-61 threshold question the agent had incorrectly closed as "not a bug" — now `docs/decisions/PENDIENTE-umbral-educacion-fisica.md`. Round 3 (follow-up 2): found `personal_proporcional` was being used for two rules needing opposite rounding directions (Inicial rounds up per COMPENDIO L422, Primaria rounds down) — added `redondeo` column; found and fixed a `tipo_regla` taxonomy leak (3 rows measuring linear metres/book counts misclassified as `superficie`) — added `infraestructura`; created `app/Domain/Validaciones/Regla/CalculadoraRequerimiento.php`, the project's first real Motor de Validación code (pure, no DB), and rewired boundary tests to exercise it instead of duplicating arithmetic inline. Round 4 (follow-up 3): added two guards to the calculator against silent-failure modes (null-threshold coercion in `personal_umbral`, silent truncation in `personal_por_espacio` — both now throw); wrote `docs/decisions/PENDIENTE-origen-de-magnitud.md`, a design memo (not implemented) on where each rule's `magnitud` input should come from and the one confirmed rule-composition case; corrected README.md/CLAUDE.md stack claims (Livewire 4→3, PHP 8.4→8.3) to match `composer.json`; marked the 2026-09-04 Decisions Log entry's `ceil` guidance (line above, this file) as SUPERSEDED by the floor fix.
- **Merged to `master`** on explicit user instruction ("Merge it") — `git merge --no-ff arch/reglas-validacion-schema` (commit `19c66c9`), zero conflicts, re-verified with `migrate:fresh --seed` + full suite (106/106) on `master` itself before pushing. Feature branch left on the remote, not deleted (not asked). User's own unrelated in-progress `.gitignore` edit (untracking `CLAUDE.md`/`docs/progress.md`) was stashed before the branch switch and restored after — left uncommitted, as found, not this session's decision to make.
- **Two decisions deliberately left open, not resolved by this session**: `docs/decisions/PENDIENTE-umbral-educacion-fisica.md` (60 vs. 61 threshold — needs SEDEQ) and `docs/decisions/PENDIENTE-origen-de-magnitud.md` (magnitude sourcing + composition — needs the architect, before Motor de Validación engine code is written). Session state saved to `~/.claude/session-data/2026-09-07-reglasvalid-session.tmp`.

### 2026-09-07 (branch `feat/wizard-progreso`)
- **Wizard progress tracking + `personal` draft rows**, explicitly authorized schema addition (two new tables, one nullability change): `pasos_captura` (catalog, six PRD §5 Paso 3 sub-steps) + `escuela_nivel_pasos` (per-expediente sub-step progress, schema/seed only — no code creates or advances rows yet); `personal`'s six non-identifying columns made nullable to support draft rows, closed by the new `app/Domain/Personal/RegistroPersonalCompleto` completeness rule (TDD, 8 tests) that the Motor de Validación's future counting code must call. Full detail and Decisions Log entries above: `docs/reports/2026-09-07-wizard-progreso.md`.
- **ADR-001 written** (`docs/decisions/ADR-001-frontera-de-capas.md`): formalizes rejecting the API spec's D2 (real-HTTP-round-trip for Livewire's internal calls) in favor of an in-process `app/Application/` layer; HTTP API still built for Etapa 3 as a thin adapter; Deptrac enforcement deferred past MVP; system confirmed server-rendered, not a SPA. `docs/superpowers/specs/2026-09-04-api-layer-design.md` D2/D3 marked superseded in place.
- Verification: `migrate:fresh --seed --force` against dev DB (user confirmed first, per CLAUDE.md data-safety rule — the task's literal instruction targeted the default `.env` connection, which is dev, not test); full suite 116/116 (106 pre-existing + 10 new). Worktree needed a from-scratch `composer install`/`npm install`/`npm run build` and copies of three gitignored local-only files (`.env`, the DDL, the API-layer spec) — none of those are shared between git worktrees.
- **Merged to `master`** on explicit user instruction ("Merge it") — `git merge --no-ff origin/feat/wizard-progreso`, zero textual code conflicts. `docs/progress.md` itself conflicted against this same unrelated stashed entry (both sessions appended at the same anchor line) — resolved by keeping both entries, this one placed after the reglas-validacion one chronologically.

### 2026-09-07 (branch `feat/paso1-preregistro`)
- **Design foundation + Paso 1 Preregistro, first vertical slice.** `tailwind.config.js` now carries every DESIGN.md token (colors incl. 7-of-8 nivel-* — `nivel-cam` excluded, `nivel-posgrado` deliberately left undefined, flagged for SEDEQ's design owner), spacing, border-radius, and typography scales, all named to match DESIGN.md 1:1. Cal Sans, Hanken Grotesk, and JetBrains Mono self-hosted from OFL-licensed npm packages into `public/fonts/` — no `cdn.jsdelivr.net` reference anywhere, verified over HTTP. Four `resources/views/components/ui/*` Blade components (text-input, button-primary, button-secondary, alert) — Paso 1's actual needs only, no hero/stat/chart/footer components built. `resources/views/layouts/tramite.blade.php` (top-nav + `<x-tramite.progreso>`, backed by a class-based component that resolves `pasos_captura`/`escuela_nivel_pasos` step state, no hardcoded step names) is now the wizard shell every step will use. First real `app/Application/<Feature>/` code: `IniciarTramiteNuevo` (DTO in, opens its own transaction, writes `planteles` then `escuelas` via two new Eloquent models `Plantel`/`Escuela` — the project's first Eloquent models), establishing the pattern the remaining 11 wizard steps copy. Full detail, token table, font licensing, and 6 undocumented UI decisions flagged for the design owner: `docs/reports/2026-09-07-paso1-preregistro.md`.
- **Two real bugs caught only by hitting the routes over HTTP, not by the test suite**: `Plantel`'s Eloquent table-name guess (`plantels`, wrong) needed an explicit `protected $table`; and Livewire's full-page `#[Layout('layouts.tramite')]` (which needs a plain view path) doesn't resolve the same way as the `<x-layouts.tramite>` component tag used in the Paso 2 placeholder view — fixed by registering `resources/views/layouts` as an anonymous-component namespace in `AppServiceProvider`, which requires the double-colon `<x-layouts::tramite>` syntax, not dot notation. `Livewire::test()` didn't exercise this path, so the Livewire component test suite passed throughout — a reminder that Livewire's page-component HTTP routing has its own untested code path.
- Chrome/chrome-devtools-mcp unavailable in this sandbox (no Chrome binary) — browser verification was curl/HTML-source only; real visual QA (Cal Sans actually rendering, spacing, focus rings, responsive layout) is deferred to manual review.
- Full suite: 124/124 (116 pre-existing + 9 new). Not merged — branch pushed, per task instructions.
- **Follow-up: closed the Blade-layer leak in the progress widget.** `<x-tramite.progreso>` originally ran its `pasos_captura`/`escuela_nivel_pasos` queries inside the `.blade.php`'s `@php` block — the project's only template-does-its-own-fetching case. Converted (`c4ea56d`) to a class-based component, `app/View/Components/Tramite/Progreso.php`, which queries in its constructor and hands the view a plain collection with each step's `estado` already resolved; the template is now pure markup. A component test (`ProgresoTest`, 2 tests) covers step ordering and completado-vs-pendiente rendering. This is read-only display data, so it deliberately does not go through an Application use case — see the boundary note in `docs/reports/2026-09-07-paso1-preregistro.md` and ADR-001. Full suite: 126/126 (124 pre-existing + 2 new).

### 2026-09-07 (branch `fix/sesion-419`) — diagnosis only, no fix
- **Diagnosed the Livewire "Page expired" (419) on Paso 1 as a component-registration bug, not a session/CSRF issue.** User reported constant 419s on every Livewire interaction (idle, editing a field, selecting an existing plantel). The task framed this around four session-config hypotheses (APP_URL/host mismatch, `SESSION_SECURE_COOKIE`, `SESSION_DOMAIN`, short `SESSION_LIFETIME`) — all four were ruled out with direct evidence: a `curl` cookie-jar reproduction showed the same session ID (verified by decrypting the cookie, not just comparing ciphertext) reused across requests; a real Livewire AJAX round-trip built via Laravel's `Http` facade, carrying a genuinely valid CSRF token and session cookie, still returned 419 — with body `Livewire\Exceptions\LivewireReleaseTokenMismatchException`, not a CSRF error. Traced to: Livewire's `ReleaseToken::verify()` looks up the snapshot's component name in `ComponentRegistry` before comparing tokens; this project's Livewire components live under `App\Http\Livewire\...` (the project's deliberate convention, not Livewire 3's auto-discovered `App\Livewire\...`), so the snapshot's auto-derived name (`app.http.livewire.tramite.paso1-preregistro`) never resolves, and nothing calls `Livewire::component(...)` to register it manually. **Every Livewire AJAX request for `Paso1Preregistro` fails unconditionally**, in any environment, regardless of session state — this is why it happens "constantly."
- **No fix applied**, per the task's own explicit instruction to stop and report rather than guess once the evidence didn't match the listed hypotheses, and because the actual fix (registering the component, or moving `app/Http/Livewire/` → `app/Livewire/`) touches wizard/Livewire code that this task placed out of scope. Full evidence and reproduction steps: `docs/reports/2026-09-07-fix-sesion-419.md`. Diagnostic report committed and pushed to `fix/sesion-419` (`3ffb403`) despite carrying no code change — the finding itself is worth keeping on record.
- **This blocks any further browser testing of Paso 1**, and will hit the same wall for every future wizard step built the same way — needs a decision (register-in-place vs. migrate the Livewire component directory) before the next wizard step is built, not just before Paso 1 is retested.
- Incidentally found a `php artisan serve` process already bound to `127.0.0.1:8000` on the main checkout (not any worktree) during this diagnosis, most likely the user's own manually-started dev server per the task's stated setup — left untouched, not investigated further.
- Root cause formalized as an open architectural decision, not left as only a Session Log bullet: `docs/decisions/ADR-003-namespace-livewire.md` (filed as `PENDIENTE-namespace-livewire.md` at the time; renamed 2026-09-09 once resolved, see below).

### 2026-09-08 (directo en `master`)
- **Merge de `fix/sesion-419` resuelto y `CLAUDE.md` actualizado.** El conflicto de `docs/progress.md` (tercera vez, misma línea ancla) se resolvió conservando ambas entradas en orden cronológico; se corrigió la descripción obsoleta de `<x-tramite.progreso>` en la entrada de Paso 1 y se agregó el bullet del seguimiento `c4ea56d`. Nuevo `docs/decisions/ADR-003-namespace-livewire.md` (filed as `PENDIENTE-namespace-livewire.md` at the time; namespace de componentes Livewire — bloquea toda prueba en navegador del wizard). Merge `7e472b6`, pusheado. Suite 126/126.
- **Nueva regla en `CLAUDE.md` (`b7e0ac0`), aplicada directo en `master` sin rama**: `docs/progress.md` tiene un solo escritor, el controlador, y solo al mergear; los agentes en worktree escriben su `docs/reports/YYYY-MM-DD-<tema>.md` y nunca tocan el ledger. Motivo: tres merges consecutivos conflictuaron en la misma línea ancla, un conflicto que no aporta información. Las decisiones abiertas van a archivo propio `PENDIENTE-<tema>.md`, no a bullet.

### 2026-09-08 (branch `worktree-fix+namespace-livewire`, merged to `master`)
- **Resolved `docs/decisions/ADR-003-namespace-livewire.md` (then still filed as `PENDIENTE-namespace-livewire.md`): migrated all 8 Livewire components from `app/Http/Livewire/` to `app/Livewire/`**, fixing the 419 diagnosed (not fixed) in `docs/reports/2026-09-07-fix-sesion-419.md`. Decision (made before the task, not re-litigated): migrate the directory rather than manually register each component — manual registration needs one `Livewire::component(...)` line per component, including the 11 wizard steps not yet built, and a single missed line reproduces the same misleading 419. Full detail: `docs/reports/2026-09-08-namespace-livewire.md`.
- **TDD proof the fix is real, not assumed**: wrote `Paso1PreregistroHttpRoundTripTest` *before* the move — a genuine HTTP round-trip to `/livewire/update` (GET the page, extract CSRF token/session cookie/`wire:snapshot`, POST them back), the code path `Livewire::test()` bypasses and the reason the rest of the suite stayed green the whole time this bug was live. Watched it fail with `LivewireReleaseTokenMismatchException` (the exact exception from the diagnosis, confirmed via a temporary response-body dump, not assumed) before migrating, then watched it pass after. Full suite: 127/127 (126 pre-existing + 1 new).
- **Scope held**: every namespace declaration and reference updated (`routes/web.php`, 4 Blade `use` imports, the existing `Paso1PreregistroTest`, both moved to `tests/Feature/Livewire/` per PSR-4); `routes/auth.php` (Volt-only) and `AppServiceProvider` correctly identified as needing no change. `app/Application/`, `app/Domain/`, `app/Models/`, and all migrations confirmed untouched.
- `docs/decisions/PENDIENTE-namespace-livewire.md` closed (`Estado: resuelto`, resolution appended, original analysis preserved) — no prior "closed" convention existed among this directory's PENDIENTE files, so one was proposed there rather than invented silently. `CLAUDE.md`'s architecture section corrected (it had things backwards — still said `app/Http/Livewire/*` was current) and now explains why the convention changed, pointing back at the decision doc. **Note (2026-09-09): this file was never renamed when closed, despite carrying `Estado: resuelto` — fixed same date, see below; it is now `docs/decisions/ADR-003-namespace-livewire.md`.**
- Independent code review (background subagent, full uncommitted diff): no Critical or Important findings, verdict "Ready to commit: Yes." Committed (`3810201`) after the review came back clean. Merged to `master` on explicit user instruction ("Merge and push") — `git merge --no-ff`, zero conflicts.

### 2026-09-08 (branch `worktree-chore+ci-github-actions`, merged to `master`)
- **Added GitHub Actions CI (`.github/workflows/ci.yml`)**: first independent, non-self-reported verification of the test suite. Three required jobs on every push/PR — `test` (PHP 8.3, Postgres 18 service container, `composer install`/`npm ci`/`npm run build`, `migrate:fresh --seed --force` against a CI-only ephemeral database, `php artisan test` with a JUnit-derived job summary), `static-analysis` (PHPStan level 5 via Larastan, plus exactly one PHPat rule — `app/Domain` must not depend on `Illuminate\*`, the first mechanical check for ADR-001), `style` (`vendor/bin/pint --test`, check-only). Full detail and deviation rationale: `docs/reports/2026-09-08-ci-github-actions.md`.
- **Postgres service credentials deliberately match already-committed `phpunit.xml`/`.env.testing` test-only defaults** (`sedeq_app`/`sedeq_app_pw`/`sedeq_incorporacion_testing`) rather than generic `postgres`/`postgres` — avoids either editing test config or fighting `phpunit.xml`'s `<env>` block, which overrides `.env` files anyway. Postgres 18 chosen to match the documented stack version; image availability unverifiable from the sandbox (no network) — a fallback-to-16 comment is left in the workflow in case the tag doesn't exist.
- **First local run surfaced 5 pre-existing PHPStan level-5 errors** (3 files: `ListarPlantelesDisponibles.php`, `VerifyEmailController.php`, `Progreso.php` — all Minor-tier type-narrowing issues, none architectural) **and 13 pre-existing Pint violations** (mechanical spacing/`new_with_parentheses` rules) — deliberately not fixed, per task scope (tooling only, no application-code changes); both jobs will show red on the first real CI run for this reason, not a workflow defect. Full list and rough fix effort in the report above.
- Independent code review (background subagent): no Critical findings; one Important finding (a deprecated PHPat method alias, `shouldNotDependOn()` → `shouldNot()->dependOn()`) fixed before commit and re-verified (same 5 pre-existing errors, no new ones). `app/Domain/`, `app/Application/`, `app/Models/`, and all migrations confirmed untouched throughout. Committed (`9ee5532`), merged to `master` via `superpowers:finishing-a-development-branch` on explicit user instruction, zero conflicts. Full suite re-verified on `master` post-merge: 127/127.
- Could not run the actual GitHub-hosted workflow from this sandbox — flagged as unverified until pushed and run for real; the Postgres image tag in particular is the most likely first-run failure point if it needs the 16 fallback.

### 2026-09-08 (directo en `master`) — first real CI run and follow-up fixes
- **First real GitHub Actions run: all three jobs failed identically at `composer install`.** Diagnosed via `gh run view`'s logs (not guessed): `composer.lock` has symfony 8.1.x packages requiring PHP `>=8.4.1`, but the workflow pinned `8.3` per `composer.json`'s declared (looser) `^8.3` constraint and CLAUDE.md's documented stack version. The lock file was generated on the local dev machine, which actually runs PHP 8.4.25 (Herd) — a pre-existing docs/lock mismatch CI exposed for the first time, not something CI itself caused. Fixed by pinning all three jobs to `php-version: '8.4'` (`92b52c3`), committed directly to `master` — trivial, single-file, mechanical. Confirmed fixed: rerun via `gh run watch` showed `test` green (127/127) and `static-analysis`/`style` red only on the already-documented pre-existing findings — CI now behaving exactly as designed.
- **Fixed all 5 pre-existing PHPStan errors and 13 Pint violations** documented in the entry above, on branch `worktree-fix+ci-pint-phpstan`. Pint: single `vendor/bin/pint` auto-fix run, purely mechanical (`new_with_parentheses`, spacing), no logic changes. PHPStan, each fixed at the cause, not suppressed: `VerifyEmailController` now uses Laravel's own `$request->fulfill()` instead of manually calling `markEmailAsVerified()`+firing `Verified` (behaviorally identical — `fulfill()` does the same two things internally, confirmed by reading the vendor source); `Plantel` got an accurate `@property` docblock (nullability checked against the `planteles` migration's actual `NOT NULL` columns) so Larastan can see its Eloquent magic properties; `ListarPlantelesDisponibles`'s return docblock narrowed `etiqueta` from `string` to `non-falsy-string`, matching what the `", "`-joined interpolation structurally always is; `Progreso` got explicit `(int)`/`(string)` casts on raw `DB::table()` row values (real coercions, not silencing — Postgres/PDO returns most columns as strings) plus a corrected `@var` docblock matching what an `(object)` cast on an array actually produces.
- Independent code review (background subagent): verified each PHPStan fix directly against Laravel/vendor source and the DDL migration rather than trusting the account, and independently reran `pint --test`/`phpstan analyse`/`php artisan test` itself rather than trusting reported numbers — all matched exactly. No Critical or Important blockers; one non-blocking note flagged for future cleanup (not done here): `Progreso`'s raw `DB::table()` query could become an Eloquent model for cleaner typing. Committed (`6ab138c`), merged to `master`, zero conflicts. Full suite + Pint + PHPStan re-verified on `master` post-merge, all clean (127/127, 0 style violations, 0 static-analysis errors) — this main checkout's `vendor/` needed its own `composer install` first, since `larastan`/`phpat` had only ever been installed inside isolated worktrees until now.

### 2026-09-08 — solicitante ownership/identity model: brainstorm + plan complete, execution not started (checkpoint before usage limit)
- **`superpowers:brainstorming`** (architectural path) on the ownership gap first flagged in the initial architecture audit: neither `planteles` nor `escuelas` had an owner column, `/tramite/preregistro` had no `auth` middleware, and `ListarPlantelesDisponibles` returned every plantel's full address unfiltered to anonymous visitors. Design approved: new `solicitantes` table as the identity/ownership layer, deliberately separate from both `users` (auth) and the already-migrated `responsables_legales` (per-trámite legal paperwork — PRD/Paso 2 conflate these two concepts under one name, this design does not). Ownership at `escuelas`, not `planteles` (planteles stay shared/unowned — matches the compendio's own plantel-centric framing of Etapa 3 reuse). SEDEQ reviewers stay on `users`, gated by a `spatie/laravel-permission` role, not a separate table/guard. Spec: `docs/superpowers/specs/2026-09-08-modelo-identidad-solicitante-design.md` (gitignored, per this directory's existing convention).
- User review added one required addition before approval: a migration-embedded backfill (`Solicitante::backfillDesdeUsers()`, called from the `solicitantes` migration's `up()`) for `users` rows that predate the auto-creation listener — stated explicitly as automatic in every environment, not a manual step, per this project's prior "agent forgot a step" incidents (Livewire namespace registration, `progress.md` anchor-line conflicts).
- **`superpowers:writing-plans`**: 8-task TDD implementation plan written, `docs/superpowers/plans/2026-09-08-modelo-identidad-solicitante.md`. User reviewed once and found 3 real issues, all fixed in place: (1) task order — the `auth`-middleware task must land before the Livewire component change that dereferences `auth()->user()->solicitante->id` with no null-check, so no individual commit is unsafe; while fixing this, also caught and documented (not glossed over) that the `IniciarTramiteNuevo` signature-change task independently breaks its Livewire caller's tests for one task's worth of commits regardless — now stated explicitly with accurate intermediate test counts rather than a false "0 failures" claim. (2) The backfill test originally used `Artisan::call('migrate:rollback', ['--step' => 1])`, which silently breaks once later tasks add their own migrations (rolls back whichever migration is actually newest, not necessarily `solicitantes`) — fixed by extracting the backfill into the static method above, testable directly, independent of migration ordering. (3) `escuelas.solicitante_id`'s FK had no declared `ON DELETE` behavior (accidental Postgres default) — made explicit (`->restrictOnDelete()`, an escuela is a real regulatory record, never silently cascade-deleted), with a test asserting it.
- `CLAUDE.md` edit made directly by the user in the IDE mid-session (not by any agent) — adds a "read the decision record before writing code" section and a CI-drift note — confirmed committed by the user (`5c1c78e`).
- **User chose subagent-driven execution (`superpowers:subagent-driven-development`).** Worktree created (`worktree-feat+identidad-solicitante`), SDD ledger at `.superpowers/sdd/2026-09-08-modelo-identidad-solicitante/progress.md` inside it, pre-flight conflict scan run and clean (no rulings needed before Task 1).
- **Task 1 complete** (commit `6119b1e`, `solicitantes` table + `Solicitante::backfillDesdeUsers()` + model + factory + `User::solicitante()`): implementer (haiku) → 131/131 (127 baseline + 4 new). Reviewer (sonnet) → spec compliant, task quality Approved, no Critical/Important. Two ⚠️ items resolved by the controller, not the implementer's fault: this worktree never received a copy of the real 650-line `docs/ddl_sistema_incorporacion_v3.sql` (only `.env` was copied during setup) — the implementer correctly appended the `solicitantes` table per the brief, but to a stub/nonexistent file; fixed by copying the real file into the worktree, reapplying the same append there, and syncing the identical append into the main checkout's own copy too (this entry). Two Minor findings parked (a no-op same-namespace `use` import, an unused `escuelas()` relation that Task 3 will exercise) — cosmetic, no action needed.
- **Task 2 complete** (commit `3c86ebb`, `CrearSolicitanteAlRegistrarUsuario` listener + `AppServiceProvider` registration): implementer (haiku) → 133/133 (131 + 2 new), covering both a direct `Registered` event dispatch and the real Volt registration flow end-to-end. Reviewer (sonnet) → spec compliant, task quality Approved, no Critical/Important. Two Minors parked (unused import in the test; the `firstOrCreate` double-fire safety isn't itself exercised by a test — not a spec gap, the brief didn't ask for it).
- Applying `solicitantes`' migration to the dev database was raised mid-session (user asked about it directly, no postgres MCP tool available in this session either) — explicitly deferred, not done: the plan's own "After all 8 tasks" section gates all dev-DB migration to after the full plan lands, and Task 3 specifically can't apply to dev until the 3 pre-existing `escuelas` rows are resolved regardless.
- **Task 3 complete** (commits `8b1105b`, `d4f2edd`, `escuelas.solicitante_id` NOT NULL with explicit `restrictOnDelete()`, `Escuela::solicitante()` relation): implementer (haiku) → found and correctly reported that the plan's own "5 pre-existing tests fail" claim was wrong.
  - **Ruling (plan defect, not implementer error)**: verified directly via `php artisan test` that only 2 of `IniciarTramiteNuevoTest`'s 5 tests actually reach `Escuela::create()`'s write path — the other 3 fail validation or a forced-exception listener first, so they were never going to break. The plan's Task 3 test-count arithmetic was simply wrong. Separately, a genuine plan gap: `tests/Feature/View/Components/Tramite/ProgresoTest.php` has its own unrelated `Escuela::create()` fixture (no task's file list ever named this file) that the same NOT NULL constraint breaks, and no later task in the 8-task plan touches it. Ruled: fold the one-line `ProgresoTest` fixture fix into Task 3 itself (same root cause it introduced), rather than leave an orphaned failure no task owns.
  - Fix round 1/5: implementer corrected the `ProgresoTest` fixture (`Solicitante::factory()->create()` + `solicitante_id`), confirmed exactly 2 failures remain (both `IniciarTramiteNuevoTest`, correctly Task 4's job) — 136 total, 134 passing.
  - Reviewer (sonnet) → spec compliant, task quality Approved, no Critical/Important, explicitly confirmed the `ProgresoTest` fix was in-scope per the ruling, not creep. One Minor parked (the `ON DELETE RESTRICT` test asserts a generic `QueryException`, not the specific Postgres SQLSTATE — would let a future `CASCADE` regression slip past as "some DB error"; not blocking).
  - **Correction for whoever runs Task 4 next**: the plan's own text still says "5 pre-existing failures" going into Task 4 — the real number is **2** (both in `IniciarTramiteNuevoTest`). Carry this correction into Task 4's dispatch.
- **Session stopped here** — 3 of 8 tasks done. Worktree kept (not removed), branch `worktree-feat+identidad-solicitante`. Resume via `superpowers:subagent-driven-development`: read the SDD ledger (`.superpowers/sdd/2026-09-08-modelo-identidad-solicitante/progress.md`) first — it names Tasks 1-3 complete, including the Task 3 ruling above — then dispatch Task 4's implementer, carrying the corrected "2 failing, not 5" count; this is a genuine mid-plan resume, not a cold start.

### 2026-09-09 — solicitante ownership/identity model: Tasks 4-8 + fix wave complete, merged to master
- **Resumed via `superpowers:subagent-driven-development`** from the 2026-09-08 checkpoint. Tasks 4-8 dispatched and reviewed one at a time (all implementers haiku, all task reviewers sonnet), each independently test-verified by the controller directly (`php artisan test`), not trusted from agent reports alone: Task 4 `IniciarTramiteNuevo::ejecutar()` takes `solicitanteId` (`ee31f17`), Task 5 `auth` middleware on the trámite routes (`ade7464`), Task 6 `Paso1Preregistro` resolves `solicitante_id` from the session, closing the interim-red window Task 4 opened (`6bb464a`, 138/138 green), Task 7 `historial_estados_expediente.usuario_sedeq` → real FK on `users` (`a387807`), Task 8 Filament panel gated behind a spatie `sedeq` role (`53c8437`, 142/142 green).
- **Three more plan test-count corrections** (Tasks 4, 5 — Task 3's was already caught 2026-09-08), same pattern each time: the plan predicted more newly-failing tests at each step than actually occurred (Task 4: predicted 4, actual 1; Task 5: predicted 4, actual 2). Controller-verified directly each time, not code defects — full detail in `docs/reports/2026-09-08-modelo-identidad-solicitante.md`.
- **Task 7 surfaced a git-hygiene finding, parked**: `docs/ddl_sistema_incorporacion_v3.sql` had never been git-tracked before Task 7's commit (`a387807`), so that commit's diff bundles every prior task's on-disk-only DDL edits into one commit instead of a scoped change. Not a code defect — a `git blame` attribution issue on a doc-only file. Not fixed (would require history surgery on a pushed branch); flagged for the user, not resolved unilaterally.
- **Final whole-branch review** (opus, full range `5c1c78e..53c8437`) returned "With fixes": 1 Critical (CI red — Pint 5 files, PHPStan level 5 2 errors, both branch-introduced; reclassified two previously-"cosmetic" parked Minors as real CI failures), 3 Important (a seeder-created test user had no `Solicitante` row → real 500 on the wizard for that login path; no session report existed yet; the DDL doc declared `escuelas` referencing `solicitantes` 366 lines before defining it, not runnable top-to-bottom), 4 Minor (parked — Paso 2 not yet owner-scoped, an IDOR-shaped gap harmless today since Paso 2 loads no record yet; `Paso1Preregistro`'s direct Eloquent relation read judged within ADR-001's spirit; migration-depends-on-model tradeoff already accepted; 2 previously-parked Task 7 test-hygiene items).
- **One fix wave** (sonnet, commits `015ee04`, `44e0558`, `cf397f0`) addressed all 4 blocking items: `getAuthIdentifier()`/`getKey()` replacing untyped `->id` access (type-correct, no suppression) cleared PHPStan; `pint` auto-fixed the 5 files; `DatabaseSeeder`'s test user now also gets a `Solicitante::factory()` row (root-fixed the one concrete gap, deliberately did **not** add a `UserFactory::afterCreating()` hook — that's a bigger invariant decision left for the user); DDL doc's `solicitantes` block moved before `escuelas`; session report written (`docs/reports/2026-09-08-modelo-identidad-solicitante.md`). Scoped re-review (sonnet) confirmed all 4 ADDRESSED, no new Critical/Important. All three CI gates green: `php artisan test` 142/142, `pint --test` 0 files, `phpstan analyse` 0 errors.
- **Closed a leftover obligation from Task 1's 2026-09-08 ruling**: the main checkout's own `docs/ddl_sistema_incorporacion_v3.sql` had never actually received the `solicitantes` append the worktree's copy got — the prior session's ruling said this would be synced "before finishing up" but the session ended first. Synced this session (superseded moments later by the merge itself, which brought in the branch's fully-reordered version anyway).
- Two stray untracked files inside the worktree removed as pre-merge housekeeping (not part of any commit, not anyone's in-progress work): a duplicate `app-laravel/docs/superpowers/{plans,specs}/` copy (real copies exist at repo-root `docs/superpowers/`) and a one-line `app-laravel/test_output.txt` debug capture.
- **Merged to `master`** on explicit user instruction ("1" — merge locally) — `git merge worktree-feat+identidad-solicitante` (ort strategy, clean, no conflicts), re-verified 142/142 on `master` post-merge. Worktree removed (`git worktree remove`), branch deleted (`git branch -d`).
- **Deliberately not done, per the plan's own gating and CLAUDE.md's data-safety rule**: dev database migration. Three pre-existing `escuelas` rows in dev block the new `solicitante_id NOT NULL` column; needs a user decision (delete vs. hand-assign) before `php artisan migrate` can run against dev, followed by confirmation and then `RolesSeeder` so a real SEDEQ account can access `/admin`. Every migration/seed run this session and the prior one targeted `sedeq_incorporacion_testing` only.
- **Two open items surfaced, not resolved**: `docs/decisions/` still has no `ADR-002` for this identity/ownership decision, though the DDL doc's comments point at one (writing it is a separate architectural task); Paso 2's not-yet-owner-scoped route (`/tramite/paso2/{escuela}`) is an IDOR-shaped gap that's harmless only until Paso 2 loads a real record — worth its own `PENDIENTE-` file before that code is written.

### 2026-09-09 (continued) — ADR-002 written, dev migration applied, login/register/forgot-password restyled
- **`docs/decisions/ADR-002-modelo-identidad-solicitante.md` written**, closing the open item above. Follows ADR-001's format; DDL doc's two stale "ADR-002, pendiente de escribirse" comments (`docs/ddl_sistema_incorporacion_v3.sql`) updated to point at the real file.
- **Dev database migration applied** — the plan's 3 gating steps, now done: user confirmed dev DB carries no real data and authorized deleting the pre-existing `escuelas` rows (found to be **7** at inspection time, not 3 as the plan's stale count said — all `plantel_id=1`, `nombre_aprobado` null, manual QA artifacts). User ran the deletion, `php artisan migrate`, and confirmed via `migrate:status` that all three solicitante-related migrations (`solicitantes`, `add_solicitante_id_to_escuelas`, `usuario_sedeq_id` FK) show `Ran`, 0 pending, `escuelas` back to 0 rows. `RolesSeeder` run against dev on explicit user request ("do it yourself") — confirmed via tinker that the `sedeq` role now exists in dev's `roles` table. No user has been assigned the role yet — `/admin` is still closed to everyone in dev until someone runs `$user->assignRole('sedeq')`.
- **Login/register/forgot-password restyled** to match Paso1's institutional design tokens (`x-ui.text-input`/`x-ui.button-primary`/`x-ui.alert`, Spanish labels, `layouts/guest.blade.php`'s shell rebuilt to match `tramite.blade.php`'s top-nav + card look), per explicit user request ("like you did with the preregistro form"). One round of visual feedback from the user (screenshot): the card was rendering full-width/no-radius — root cause was `npm run dev`/`build` never running (`php artisan serve` alone doesn't compile Vite/Tailwind assets, so newly-added classes never reached the compiled CSS); confirmed by asking what was actually running. Also swapped an arbitrary-bracket `max-w-[420px]` for the standard `max-w-md` utility and switched the auth shell to vertically+horizontally centered (matching the user's reference screenshot of the old style), on the theory that a purge/JIT edge case on the bracket value was a secondary contributor. User re-ran `npm run build`; not yet re-confirmed visually in this session (no browser available here — flagged in an earlier session too).
- Auth test suite (21/21) and full suite (142/142) reverified after each of the above changes.
- **Next steps, not started**: assign the `sedeq` role to a real dev account so `/admin` is reachable; a `PENDIENTE-` note for Paso 2's not-yet-owner-scoped route (flagged in the final review, still open); build Paso 2 (Responsable) itself, still a stub component.

### 2026-09-09 (continued, directo en `master`) — sedeq role assigned in dev, post-login redirect fixed
- **`sedeq` role assigned to the dev test account** (`test@example.com`, id 1) — user ran the tinker command directly (destructive dev-DB write, per CLAUDE.md's data-safety rule and this session's auto-mode classifier blocking such writes from the agent). Confirmed visually: `/admin` renders the Filament panel dashboard for that account.
- **Fixed post-login/register redirect, previously untouched Breeze default (`route('dashboard')`)**: user caught, after logging in, that the app landed on Breeze's stock `/dashboard` placeholder instead of anywhere trámite-specific. Since `users` serves both solicitantes and SEDEQ staff (per ADR-002), the fix is role-based, not a single hardcoded target — `login.blade.php` now sends `sedeq`-role users to `/admin` and everyone else to `/tramite/preregistro`; `register.blade.php` always sends to `/tramite/preregistro` (a fresh registration can never carry the `sedeq` role). TDD: updated `AuthenticationTest`/`RegistrationTest` assertions to the new targets, added `test_sedeq_users_are_redirected_to_the_admin_panel_after_login`. Full suite 143/143, Pint 0, PHPStan 0 errors.
- **`dashboard` route/view left in place but now unreferenced by either auth flow** — not removed, wasn't asked, and something else (e.g. the nav "Profile" dropdown) may still point at it; worth a follow-up check before Paso 2 work if it turns out to be dead.
- **Filed `docs/decisions/PENDIENTE-paso2-owner-scoping.md`**: `/tramite/paso2/{escuela}` is `auth`-gated but not owner-gated — any authenticated user can substitute another solicitante's escuela id, an IDOR-shaped gap in enforcing ADR-002's `Escuela belongs to Solicitante` model. Harmless today only because Paso 2 is a placeholder that reads/writes nothing; must be closed before Paso 2 gets real logic. Fix shape stated (compare `escuela.solicitante_id` against `auth()->user()->solicitante->id`), mechanism (Policy vs. inline check vs. scoped route-model-binding) deliberately left to the architect.

### 2026-09-09/10 (branch `worktree-paso2-responsable-niveles`, merged to `master`)
- **Paso 2a complete: responsable legal (2.1) + selección de niveles (2.3)**, 13-task plan (`docs/superpowers/plans/2026-09-09-paso2-responsable-niveles.md`) executed via `superpowers:subagent-driven-development`, each task implementer→reviewer (haiku/sonnet mix per task complexity), zero Critical/Important findings across all 13. New models `NivelEducativo`, `EscuelaNivel`, `ResponsableLegal`+`PersonaFisica`/`PersonaMoral`/`Gestor` subtypes (non-standard PKs/timestamps per DDL, verified line-by-line); `RegistrarResponsableLegal`/`RegistrarNivelesSeleccionados` Application-layer use cases (ADR-001); `Paso2Responsable` two-phase Livewire component; `Paso3Placeholder` landing route/component. Full task-by-task detail: `docs/reports/2026-09-09-paso2-responsable-niveles-inicio.md`, `docs/reports/2026-09-09-paso2-responsable-niveles-progreso.md`, `docs/reports/2026-09-10-paso2-responsable-niveles-final.md`.
- **Closes `docs/decisions/paso2-owner-scoping.md`** (the IDOR gap from the entry above) — first Policy classes in the codebase, `EscuelaPolicy`/`EscuelaNivelPolicy` delegating to pure `app/Application/Escuelas/VerificarPropietarioEscuela(Nivel)` decision classes, enforced via `can:view,...` middleware + implicit route-model binding, not a raw client id anywhere. File renamed from `PENDIENTE-paso2-owner-scoping.md` at merge time (content already said `Estado: resuelto`, per this project's rename-on-close convention from ADR-003).
- **Final whole-branch review** (opus, full branch diff): verdict "With fixes" — architecture confirmed sound branch-wide (ADR-001/002/003 all hold), 3 Important findings found real: (1) `RegistrarNivelesSeleccionados` had no server-side whitelist restricting selectable niveles to Educación Básica — only the Blade checklist did, and the seeder includes `media_superior`/`superior`/`posgrado` too; (2) double-submit on either write path threw an uncaught `QueryException` against a UNIQUE constraint (`responsables_legales.escuela_id`, `escuela_niveles`); (3) `Paso2Responsable::mount()`'s redirect-to-paso3 branch (added by an earlier review specifically to dodge #2's UNIQUE) had no test. One fix wave (sonnet, 3 commits) closed all three plus 3 batched one-line Minors (missing `estado_id` assertion, an unreachable post-`expectException` assertion, missing `orderBy('id')` on two lookups); scoped re-review verdicted all 6 ADDRESSED, no new breakage.
- **Two Minor findings deliberately parked, not fixed — need a human scope call**: `domicilio_notificaciones`/`persona_autorizada_recoger` are plumbed through the DTO and `ResponsableLegal` model but have no Form field or Blade input, always `null`; 9 `PersonaMoralForm` fields + 2 `PersonaFisicaForm` fields are validated but likewise unwired to any input. Reviewer couldn't confirm from PRD §5 Paso 2.1 / the Formato de Solicitud PDF whether these are required scope or deliberate MVP slimming — not load-bearing for anything else in this branch, safe to leave open, but worth resolving before this ships.
- Full suite 172/172 (168 pre-fix-wave + 4 new from the fix wave), Pint/PHPStan clean, re-verified on `master` post-merge. Merged via `git merge worktree-paso2-responsable-niveles --no-edit` (ort strategy, zero conflicts) on explicit user instruction. Worktree removed, branch deleted.
- **Next steps, not started**: Paso 2.2 (documentos + PDF) — deliberately deferred from this plan's scope, its own future brainstorm; Paso 3 real design (currently only a placeholder landing page).

### 2026-09-10 (branch `worktree-paso2-campos-faltantes`, merged to `master`)
- **Closed the two parked Minor findings from the entry above.** User confirmed `domicilio_notificaciones`/`persona_autorizada_recoger` and all 11 previously-unwired `PersonaMoralForm`/`PersonaFisicaForm`/`GestorForm` fields are required scope, not deliberate MVP slimming. Verified against `docs/COMPENDIO_MAESTRO_Sistema_Incorporacion.md:92` before implementing: "el Formato de Solicitud se genera... a partir de los datos capturados (nombre, domicilio, tipo de persona, datos notariales si aplica)" confirms domicilio + notarial data are real captured fields feeding the PDF, not invented ones. `persona_autorizada_recoger` has no explicit compendio confirmation but was included on the user's direct instruction.
- Pure presentation-layer fix, TDD (red→green): the DTO/model/use case already carried both top-level fields end to end from the original plan — only the Form objects and Blade view never collected or displayed them. Added `domicilioNotificaciones`/`personaAutorizadaRecoger` as two new shared properties on `Paso2Responsable` (not tied to any `tipoPersona` variant), `domicilioNotificaciones` validated `required` (per the compendio confirmation above), `personaAutorizadaRecoger` `nullable` (no normative source pinning it required). Wired the remaining 11 already-validated fields into the Blade view: `personaFisicaForm.fechaNacimiento`; `personaMoralForm`'s 7 escritura/notario/RPP fields; `gestorForm`'s notario+fecha fields. 5 new tests. Full suite 176/176, Pint/PHPStan clean.
- Merged via `git merge worktree-paso2-campos-faltantes --no-edit` (fast-forward, zero conflicts) on explicit user instruction. Worktree removed, branch deleted. Full detail: `docs/reports/2026-09-10-paso2-campos-faltantes.md`.
- **Left open**: `persona_autorizada_recoger` stays `nullable` pending explicit SEDEQ confirmation it's required — a one-line change if so.

### 2026-09-10 (branch `worktree-paso2-rfc-curp-uppercase`, merged to `master`)
- RFC/CURP on `Paso2Responsable` now normalize to uppercase on save (`mb_strtoupper`, server-side, canonical regardless of what the client sends) plus a Tailwind `uppercase` class on both inputs for live visual feedback while typing. TDD (1 new test). Full suite 177/177, Pint/PHPStan clean. Fast-forward merge, worktree/branch cleaned up.

### 2026-09-10 (branch `worktree-paso1-reusar-escuela`, merged to `master`)
- **Fixed the root cause of "Paso 2 looks empty after going back to select a plantel"**: `IniciarTramiteNuevo::ejecutar()` always did `Escuela::create(...)`, for both `bifurcacion` branches — picking the same existing plantel twice silently created a second, independent `escuela` row for the same solicitante instead of continuing the trámite already in progress. Fixed with `Escuela::firstOrCreate(['plantel_id' => ..., 'solicitante_id' => ...])`, same idempotency pattern already used in `RegistrarResponsableLegal`/`RegistrarNivelesSeleccionados`. Verified two different solicitantes on the same plantel still get independent escuelas. `bifurcacion=nuevo` unaffected in practice (always a fresh plantel, so no match to find).
- **Full validation audit of every wizard form** against the DDL's actual column lengths/CHECK constraints (`Paso1Preregistro`, `PersonaFisicaForm`, `PersonaMoralForm`, `GestorForm`, `Paso2Responsable`'s inline rules). Everything already matched exactly except two real gaps in `Paso2Responsable`: `tipoPersona` had no `in:` rule (a tampered value fell through to `RegistrarResponsableLegal`'s uncaught `InvalidArgumentException` → 500 instead of a graceful validation error); `nivelesSeleccionados.*` had no per-element rule (a non-integer entry was only caught downstream by the whitelist's `array_diff`, with a misleading error message). Both fixed.
- 4 new tests, full suite 181/181, Pint/PHPStan clean (PHPStan needs `--memory-limit=512M` in this environment — 128M default isn't enough for the full project, not a code finding). Fast-forward merge, worktree/branch cleaned up. Full detail: `docs/reports/2026-09-10-paso1-reusar-escuela-y-auditoria-validacion.md`.
- **Not reviewed**: Breeze's auth forms (login/register/forgot-password) — audit scoped to the trámite wizard's own business-logic forms.

### 2026-09-10 (branch `worktree-tramite-logout`, merged to `master`)
- **Added a working logout button to the trámite wizard's top nav.** No `POST /logout` route existed anywhere in the app — the only functional logout was Breeze's stock Volt `layout.navigation` component, itself unreferenced since `/dashboard` stopped being any login/register target (see 2026-09-09 entry). `top-nav.blade.php` (used by every trámite page) is a classless Blade component, so it never could have had its own `wire:click`. Fixed with a plain `Route::post('logout', ...)` in `routes/auth.php` reusing the existing `Logout` action, and a plain HTML POST form (`@csrf` + `x-ui.button-secondary`, wrapped `@auth`) in the nav — no new Livewire component needed. 2 new tests. Full suite 183/183, Pint/PHPStan clean.
- **Not touched**: the dead `layout.navigation`/`/dashboard` pair (still unreferenced, same as before); Filament's `/admin` panel (already ships its own logout in the user menu by default).

### 2026-09-10 (branch `worktree-paso2-terna-nombres`, merged to `master`)
- **Added capture of the terna de nombres propuestos (Propuesta 1/2/3) to Paso 2a**, per a task brief confirming COMPENDIO_MAESTRO's framing (`COMPENDIO_MAESTRO_Sistema_Incorporacion.md:121`) that this is the same data later referenced in Paso 3/Anexo 2, not a second capture.
- **Plan-defect ruling**: the task brief assumed no schema existed for this and instructed adding new columns to `escuelas`. Reading `docs/ddl_sistema_incorporacion_v3.sql` first (per this file's own decision-record-first rule) found `ternas_nombres` already exists there and is already migrated (`2026_01_01_000022_create_ternas_nombres_table.php`, part of the original 44-table port) — properly normalized (one row per proposal 1-3, `UNIQUE(escuela_id, numero_propuesta)`), with `valido_marca_comercial`/`valido_registro_sedeq` columns already matching COMPENDIO's two system-checkable validations (IMPI, SEDEQ's own registry). Ruling: use the existing table, no new migration, no DDL edit — only the model and application code were missing.
- New `TernaNombre` model (`const UPDATED_AT = null`, same pattern as `Gestor`), `Escuela::ternasNombres()` relation. `RegistrarResponsableLegal` writes all three rows in the same transaction as the rest of responsable-legal data, skipping any null proposal rather than violate the column's NOT NULL (matches how every other optional DTO field already behaves — kept pre-existing use-case tests green without forcing them to supply unrelated data). All three required at the Livewire layer, per the source document's "terna" (fixed set of three) framing — no text found distinguishing partial vs. full requiredness.
- 7 new/edited tests. Full suite 187/187, Pint/PHPStan clean. Merged (fast-forward) and pushed to `origin/master` per the task's own explicit closing instruction.
- **Deliberately not built**: no real IMPI/SEDEQ-registry validation logic (`valido_marca_comercial`/`valido_registro_sedeq` stay `NULL`, to be filled by a future SEDEQ review flow, not solicitante capture); no Paso 3 consumption of this data (Paso 3 is still only a placeholder). `escuelas.nombre_aprobado` confirmed untouched — different concept (SEDEQ's final approved name, singular), not merged or aliased with the three proposals.

### 2026-09-10 (branch `worktree-fix-escuela-dedup-scoping`, merged to `master`)
- **Corrected the same-day `a7b35c8` escuela-reuse fix — it was too aggressive.** Confirmed with the domain owner: the same solicitante CAN legitimately register multiple distinct escuelas at the same plantel (e.g. two separately-named schools sharing one campus). `a7b35c8`'s `Escuela::firstOrCreate` scoped only on `(plantel_id, solicitante_id)` made that structurally impossible — any second Paso 1 visit for the same plantel by the same solicitante silently reused the first escuela forever, regardless of whether it was already a completed registration.
- **The real distinction**: whether the existing escuela has progressed past preregistro. An escuela with no `responsables_legales` row yet is still in-progress and safe to reuse (this preserves `a7b35c8`'s original point — repeat Paso 1 visits before finishing don't spawn duplicates); one that already has a `responsables_legales` row is a completed, independent registration and must get a new escuela on the next visit. Fixed via `Escuela::where(plantel_id, solicitante_id)->whereDoesntHave('responsableLegal')->first() ?? Escuela::create(...)`, using the existing `responsableLegal()` relation — no raw Eloquent leaking outside the Application layer (ADR-001).
- 1 new test (completes Paso 2 for the first escuela, then revisits Paso 1 with the same plantel/solicitante — asserts a second, distinct escuela). Both `a7b35c8`'s own tests kept passing unchanged (neither ever created a responsable, so both scenarios stayed "in progress both times", exactly as the correction predicted). Full suite 188/188, Pint/PHPStan clean. Full detail: `docs/reports/2026-09-10-fix-escuela-dedup-scoping.md`.

### 2026-09-10 — Paso 2.2 (documentos + Formato de Solicitud): brainstorm + plan complete, execution not started (checkpoint)
- **`superpowers:brainstorming`** (architectural path) on Paso 2.2, the piece deferred since Paso 2a's own scope note (2026-09-09 entry above). Design confirmed against COMPENDIO_MAESTRO and the existing DDL rather than invented: the checklist is **6 documents, not 7** (INE; acta de nacimiento/escritura-poder de facultades, mutually exclusive by tipo_persona; escritura del inmueble; Dictamen de Uso de Suelo; Constancia de Seguridad Estructural+DRO — already fused per `COMPENDIO_MAESTRO_Sistema_Incorporacion.md:86` and the already-migrated `constancias_seguridad_estructural` extension table; Formato de Solicitud). A real conflict surfaced mid-brainstorm and was resolved with the user: "Formato de pago de derechos" (RecaudaNet) appears in COMPENDIO's Paso 2.2 checklist table but its extension table (`recibos_pago_derechos`) is keyed to `documento_escuela_nivel_id`, which can't exist until *after* niveles selection (2.3) — ruled as a Paso 3 document (per escuela_nivel), out of this design's scope, not a schema defect.
- **Data model needs no new migration** — `documentos_plantel`/`documentos_escuela` base tables + `constancias_seguridad_estructural`/`acreditaciones_ocupacion_legal` extension tables were already fully designed and migrated as part of the original 44-table port; only the Eloquent models and Application-layer code are missing. `tipos_documentos.vigencia_max_dias` already generalizes the "expires after N days" rule (matches Dictamen de Uso de Suelo's 1-month window) with no per-document hardcoding needed.
- **Deliberate correction of Paso 2a's own gating logic**: COMPENDIO confirms niveles selection is "la última acción del Paso 2, después de capturar los documentos" (`COMPENDIO_MAESTRO_Sistema_Incorporacion.md:61`) — Paso 2a's `Paso2Responsable::mount()` transitions straight from `responsable` to `niveles` only because 2.2 didn't exist yet, not because the order was actually different. This plan inserts the documentos gate between them.
- **Design decisions made explicit, not left implicit**: storage is local disk for the MVP (`storage/app/documentos`, policy-gated download route reusing `EscuelaPolicy`, no `PlantelPolicy` needed since `Plantel` is shared/unowned by ADR-002) — external/cloud storage explicitly noted as a future consideration, not built now; PDF integrity hash explicitly deferred (no consumer for it until Etapa 2's review flow exists); vigencia checked at submission only, not per-upload; cross-document identity/domicilio matching is manual SEDEQ review, not automated; `estado_validacion` resets to `pendiente` on every re-upload, no exception; one Form object per document type with structured fields (`DictamenUsoSueloForm`, `ConstanciaSeguridadForm`, `AcreditacionOcupacionForm`), same reasoning as 2.1's `tipo_persona` Forms; one shared `DocumentosCompletos` Application-layer query (not duplicated inline) answers "are the 6 documents done" for both `Paso2Responsable::mount()` and the new `Paso2Documentos::mount()`.
- Spec: `docs/superpowers/specs/2026-09-10-paso2-documentos-design.md` (gitignored, per this directory's existing convention). Plan: `docs/superpowers/plans/2026-09-10-paso2-documentos.md`, 15 TDD tasks, self-reviewed for spec coverage/placeholders/type consistency.
- **Not started**: no code, no migration, no route touched this session — execution was explicitly out of scope until the plan was approved. Next session resumes via `superpowers:subagent-driven-development` (chosen, not yet dispatched) against the plan above, starting at Task 1 (`TiposDocumentosSeeder`).

### 2026-09-11 (branch `worktree-paso2-documentos`, merged to `master`)
- **Paso 2.2 (documentos + Formato de Solicitud) fully implemented**, all 15 tasks from the plan above, via `superpowers:subagent-driven-development` — each task implementer→reviewer (sonnet, cheap-tier for mechanical transcription tasks), zero Critical findings at task level. Delivers: `TiposDocumentosSeeder`; `TipoDocumento`/`DocumentoPlantel`/`DocumentoEscuela` models; `ConstanciaSeguridadEstructural`/`AcreditacionOcupacionLegal` extension models; `documentos` filesystem disk + `AlmacenDocumentos` interface/local impl; `RegistrarDocumento` generic use case (dispatches by clave, single transaction for base+extension writes); `DocumentosCompletos` (single shared completeness query, called by both `Paso2Responsable::mount()` and `Paso2Documentos::mount()`, never duplicated — closes the exact coordination risk the 2026-09-10 design session flagged); `ValidarVigenciaDocumentos` (submission-time-only vigencia check); the documentos gate inserted into `Paso2Responsable::mount()` (closes the gating-order correction the 2026-09-10 session planned); `Paso2Documentos` Livewire component (file-only → structured-field → Formato de Solicitud PDF via dompdf → vigencia enforcement); policy-gated document download route; a real-HTTP round-trip test per ADR-003's precedent. No new migrations — all 6 relevant tables already existed from the original DDL port. Full detail, task-by-task: `docs/reports/2026-09-11-paso2-documentos.md`.
- **Two real bugs found and fixed mid-plan** (both DDL/vendor-source-verified, not guessed): a `RegistrarDocumento` NOT NULL violation on `acreditaciones_ocupacion_legal.tipo` when `tipoAcreditacion` was null (Task 7); a placeholder route from Task 9 (`fn () => abort(501)`, zero params) that broke implicit route-model-binding for `can:view,escuela` — closed when Task 10 registered the real typed Livewire component in its place.
- **Final whole-branch review (opus) found a coupled Critical pair invisible to any single task's review**: `Paso2Documentos::mount()` crashed (`TypeError`) if a solicitante re-entered the documentos screen after completing all 6 documents (undefined-array-key `$pendientes[0]`); and `ValidarVigenciaDocumentos` ran only inside `Paso2Documentos::avanzar()`, never in `Paso2Responsable::mount()`, so an expired Dictamen de Uso de Suelo was bypassable by navigating directly to `/tramite/paso2/{escuela}`. Fixed together in one wave (also closed: a client-writable Livewire `$fase` property could be used to bypass structured-document capture; a missing null-guard in the Formato de Solicitud PDF blade) — `ValidarVigenciaDocumentos::ejecutar()`'s return type changed from `list<string>` to an associative `array<clave,mensaje>` as part of this fix, re-verified safe against its one existing consumer and Task 8's own tests. Scoped re-review confirmed all four ADDRESSED, no new breakage. Full suite 229/229, Pint/PHPStan clean.
- **One item the final review explicitly required be tracked, not silently parked**: the `escritura_inmueble` (Escritura del inmueble) Blade form ships functionally incomplete — exposes only 2 of `AcreditacionOcupacionForm`'s 13 fields and no `tipo` selector, so a solicitante whose property is under `arrendamiento`/`comodato`/`otro` (or who needs to submit notario/RPP/contrato data) cannot do so through this screen as shipped. Nothing on disk is wrong — the Form class and use case already accept and persist all 13 fields — this is purely a blade-completion gap traceable to the original 2026-09-10 plan's own Step 3 snippet. **Needs a follow-up task before Paso 2.2 is exercised by a real solicitante with anything other than a straightforward `escritura_publica`.**
- Merged via `git merge worktree-paso2-documentos --no-edit` (fast-forward, zero conflicts) on explicit user instruction ("1" — merge locally). Re-verified 229/229 on `master` post-merge (main checkout needed its own `composer install` first). Worktree removed (hit a Windows long-path failure on `git worktree remove`'s own delete, resolved via PowerShell `Remove-Item` + `git worktree prune`), branch deleted.
- **Other items still open from prior sessions, untouched here**: `persona_autorizada_recoger` required-ness (2026-09-10); Paso 3's real design (still only a placeholder landing page).

### 2026-09-11 (continued, branch `worktree-paso2-escritura-inmueble`, merged to `master`)
- **Closed the `escritura_inmueble` tracked gap from the entry above.** User independently re-verified the gap against actual code (not just the report) before authorizing the task, and caught a second real gap the original report/task brief both missed: `observaciones` is a real `acreditaciones_ocupacion_legal` column, absent from `AcreditacionOcupacionForm`/`DatosDocumento`/`RegistrarDocumento`'s write despite the model's `$fillable` already listing it — folded into the same task.
- Added the `tipo` selector (4 variants) to the Blade view, conditional fields per variant verified 1:1 against the DDL, `AcreditacionOcupacionForm::rules()` now branches required-ness by `tipo` (previously every field was unconditionally nullable regardless of variant — a second undocumented gap), and `observaciones` wired end-to-end. 6 new tests (all 4 variants + negative-validation for `escritura_publica`/`otro`/`arrendamiento`-`comodato` + `observaciones` round-trip). Full suite 234/234, Pint clean, PHPStan 0 errors. Full detail (in Spanish, per this session's new language convention): `docs/reports/2026-09-11-paso2-escritura-inmueble.md`.
- Independent review (sonnet) before merge: Approved, one Important finding (missing negative-validation test for the `arrendamiento`/`comodato` branch) closed in a follow-up commit before merging; two Minor findings accepted as-is.
- Merged via `git merge worktree-paso2-escritura-inmueble --no-edit` (true merge, not fast-forward — local `master` had diverged with an unrelated user-authored CLAUDE.md commit in between), zero conflicts, re-verified 234/234 on `master` post-merge. Worktree removed (same Windows long-path workaround as the prior merge: PowerShell `Remove-Item` + `git worktree prune`), branch deleted.
- **PRD corrected + new PENDIENTE filed (docs-only, direct to `master`)**: a shared-campus-different-owners counterexample (turno matutino/vespertino, different `Solicitante`s) surfaced during architecture review, and verifying it against COMPENDIO found that plantel domicilio changes require "solicitud y autorización previa de la Dirección de Educación" — not a free-form field. Corrected an earlier informal assumption in `docs/PRD_Sistema_Incorporacion_MVP.md` (Paso 3, Datos del inmueble sub-step) that domicilio was freely editable on plantel reuse — now locked to read-only unless the plantel is being registered for the first time. Filed `docs/decisions/PENDIENTE-plantel-solicitante-cardinalidad.md` for the still-open question the counterexample raised (can one `Plantel` serve `Escuela`s from different `Solicitante`s, or should it be 1:1) — confirmed unconstrained in code today (`planteles` has no `solicitante_id`, `escuelas.solicitante_id` allows any solicitante onto any plantel, already tested), and confirmed the PRD fix above already closes the concrete data-integrity risk independent of how this cardinality question resolves.
- **Three more COMPENDIO/PRD decisions closed (docs-only, direct to `master`)**: Auditorio's classification (COMPENDIO §"Infraestructura del nivel educativo" + §7 pendientes) resolved per `ANEXO_2_Instalaciones.docx`'s actual table structure — belongs under instalaciones especiales/adicionales, not actividades físicas y recreativas; removed from the pendientes list. Mobiliario-por-nivel reframed from an open question to a confirmed MVP scope decision (Educación Inicial only, other niveles deliberately out of scope pending a future SEDEQ-confirmed catalog) in both COMPENDIO and PRD. PRD §5 Paso 3 also gained an infraestructura-del-nivel note verified against the schema: `instalaciones_espacios`/`sanitarios` are anchored to `plantel_id`, not `escuela_nivel_id` (only `aulas_nivel` is genuinely nivel-scoped) — so infraestructura now follows the same plantel-reuse read-only rule as domicilio.

### 2026-09-14 (branch `worktree-plantel-owner-scoping`)
- **`ListarPlantelesDisponibles` scoped to the authenticated solicitante's own planteles**, an interim tightening (not a resolution) of the gap `docs/decisions/PENDIENTE-plantel-solicitante-cardinalidad.md` flagged: this listing was the de facto mechanism through which cross-solicitante plantel-sharing happened by default rather than by decision. `ejecutar()` now takes `int $solicitanteId` and filters via a new `Plantel::escuelas()` relation (`whereHas('escuelas', ...solicitante_id = $solicitanteId)`); `Paso1Preregistro::render()` resolves it server-side (`auth()->user()->solicitante->getKey()`), same pattern already used in `guardar()`. TDD: 2 new tests (`ListarPlantelesDisponiblesTest`) — own-plantel-only visibility, empty list (not an error) for a solicitante with zero escuelas. Full suite 236/236 (234 pre-existing + 2 new), Pint clean, PHPStan 0 errors.
- `planteles`/`escuelas` schema untouched — query-level filter only, no new column.
- Both `docs/decisions/PENDIENTE-plantel-solicitante-cardinalidad.md` (new "Actualización (2026-09-14)" section) and `docs/decisions/ADR-002-modelo-identidad-solicitante.md` §3 (correction appended below point 5, original reasoning preserved) updated to reflect the new filtered behavior and cross-reference each other — the cardinality question itself remains unresolved.

### 2026-09-14 (continued, direct to `master`, docs-only — brainstorm, no code)
- **`superpowers:brainstorming` (architectural path) for Paso 3 sub-pasos 1-3** (Datos del inmueble, Infraestructura del nivel, Mobiliario) — Plan de estudios/Plantilla docente/Matrícula explicitly out of scope (real SEDEQ information gaps, not a technical deferral). Design doc: `docs/superpowers/specs/2026-09-14-paso3-inmueble-infraestructura-mobiliario-design.md` (gitignored, per existing convention). No code/migration/model/route touched.
- **User caught two design defects during review** that the initial proposal missed: (1) `modalidad` was initially included in sub-paso 1's design — COMPENDIO confirms it applies only to Media Superior/Superior, out of this MVP's Educación Básica scope; dropped entirely, nothing to reference since its generic home (sub-paso 4) is also deferred. (2) The initial auto-complete/skip-forward proposal had `Livewire::mount()` write `escuela_nivel_pasos` directly — a real ADR-001 violation (component writing past Application). Fixed with a new shared `MarcarPasoCompletado::ejecutar(int $escuelaNivelId, string $pasoClave)` use case that `mount()` may *decide* to call but never writes to directly; every `escuela_nivel_pasos` completion (normal path and auto-complete/skip-forward) happens inside the same `DB::transaction()` as the primary write, same pattern as `IniciarTramiteNuevo`.
- **User also surfaced a live, unrelated-to-this-design defect during review, verified directly**: no existing Livewire component (including current `Paso3Placeholder`) ever sets the `$escuelaNivelId` variable that `layouts/tramite.blade.php` reads for `<x-tramite.progreso :escuela-nivel-id="$escuelaNivelId ?? null" />` — the progress widget has been silently showing all 6 sub-pasos as "pendiente" unconditionally since `Paso3Placeholder` existed, not merely unused. Folded into the design as a required fix: every new component (the 3 sub-pasos + a new `Paso3ProximosPasos` landing) must expose `$escuelaNivelId`.
- **Key design decisions**: `/tramite/paso3/{escuelaNivel}` stops being `Paso3Placeholder` and becomes the real sub-paso 1 component (`Paso3Placeholder` deleted once replaced, not left dead); new `/tramite/paso3/{escuelaNivel}/proximos-pasos` route/component lands after sub-paso 3. One Livewire component per sub-paso (3 total), one Application use case per sub-paso (`RegistrarDatosInmueble`, `RegistrarInfraestructuraNivel`, `RegistrarMobiliarioNivel`) plus the shared `MarcarPasoCompletado`. Bodega's `tipo` field reuses `instalaciones_espacios.destinado_a` (no schema change). `escritura_inmueble`/`constancia_seguridad_estructural` (already captured in Paso 2.2) shown as plain read-only inline text in sub-paso 1 for context — explicitly flagged as the project's first "cross-step read-only display" and NOT a precedent for a bigger document-review feature without a separate decision. Multi-nivel auto-complete for plantel-scoped sub-pasos 1-2 (second `escuela_nivel` on the same `Escuela` skips re-capturing already-captured plantel data). `EscuelaNivelPolicy`/`VerificarPropietarioEscuelaNivel` reused as-is, no new policy class.
- **Not started**: `superpowers:writing-plans` (next step, pending user's review of the written spec); any code, migration, or route change.

### 2026-09-21 (branch `worktree-paso3-sub-pasos-1-3`, merged to `master` at `5b02b62` — local only, NOT pushed)
- **Paso 3 sub-pasos 1-3 shipped** (Datos del inmueble, Infraestructura del nivel, Mobiliario) plus a terminal `Paso3ProximosPasos` landing, executed from `docs/superpowers/plans/2026-09-21-paso3-inmueble-infraestructura-mobiliario.md` (gitignored) via `writing-plans` → `subagent-driven-development` in a separate worktree: 11 tasks, one implementer and one reviewer each (sonnet), a whole-branch review on opus, then one fix wave. Full detail (Spanish): `docs/reports/2026-09-21-paso3-sub-pasos-1-3.md`.
- **Evidence**: suite 236 → 302 (671 assertions), Pint clean, PHPStan level 5 + PHPat 0 errors, all re-verified on merged `master`. **No migrations** — 54 ran, 0 pending; every column written already existed in the 44-table schema.
- **What landed**: 4 Livewire components (`Paso3/DatosInmueble`, `Paso3/InfraestructuraNivel`, `Paso3/MobiliarioNivel`, `Paso3ProximosPasos`), 4 use cases (`RegistrarDatosInmueble`, `RegistrarInfraestructuraNivel`, `RegistrarMobiliarioNivel`, and `MarcarPasoCompletado` — now the single writer of `escuela_nivel_pasos`), 2 plantel-reuse predicates (`DatosInmuebleYaCapturados`, `InfraestructuraYaCapturada`), 9 new models, 2 seeders. `/tramite/paso3/{escuelaNivel}` now serves the real sub-paso 1; route `tramite.paso3-placeholder` became `tramite.paso3-inmueble` (`Paso2Responsable` and its test updated in the same commit). `Paso3Placeholder` and three empty Phase-2 stubs deleted; `PlanEstudios`/`PlantillaDocente`/`Matricula` stubs untouched.
- **Three catalogs the spec assumed existed were not seeded**: `tipos_espacios` (26 rows), `niveles_tipos_espacios` (88 = 36 explicit per COMPENDIO + 52 inferred, every row `obligatorio=false` since COMPENDIO never marks any espacio obligatorio), `mobiliario_conceptos` (41, Educación Inicial; Sala de Usos Múltiples concepts carry `sala_id=NULL`; "material didáctico" has no numeric ratio in the source so it is `fijo_por_sala`/1 with the reason in `fuente`). Both seeders were run against the dev DB on the user's instruction; counts confirmed 26/88/41.
- **Live defect closed**: no component ever supplied `$escuelaNivelId` to `layouts/tramite.blade.php`, so the progress widget showed all six sub-pasos as "pendiente" unconditionally. The spec's proposed fix (a public property) does not work in Livewire 3.8 — layouts render from `PageComponentConfig->params`. The working fix is the `layoutData(['escuelaNivelId' => ...])` view macro, used by all four new components.
- **Spec deviations decided before implementation**: `RegistrarDatosInmueble` takes `escuelaNivelId` as well as `plantelId` (needed to mark the paso); auto-complete on plantel reuse applies to sub-paso 1 only (sub-paso 2 still owes its nivel-scoped `aulas_nivel`); sanitario categories per nivel are an inference (Inicial → `alumnado_maternal`,`personal`; others → the four masculino/femenino) from COMPENDIO plus the DDL's extra enum values.
- **Five controller rulings, all upheld by the final review**: (1) Larastan gap fixed at the root with `@property` docblocks on `EscuelaNivel`/`NivelEducativo` (same remedy `Plantel` got 2026-09-08) instead of a per-call-site join; (2) `MobiliarioGrupo` view-model DTO accepted over the plan's `(object)` cast, which PHPStan cannot type; (3) `x-ui.button-primary` accepted (`x-ui.button` does not exist); (4) FK correction accepted — the Paso 2.2 documents shown as context are plantel-scoped (`documento_plantel_id`), not escuela-scoped; (5) two redundant `array_values()` wrappers removed.
- **Final review (opus) found and the fix wave closed** (`16882d1`, `c2f8ce0`): the `InfraestructuraYaCapturada` sentinel keyed only on `instalaciones_espacios`, so a sanitarios-only submit could be saved twice and duplicate every sanitario row (the controller had wrongly deferred this as a minor — the "every plantel has administrative espacios" rationale is enforced nowhere); Bodega `tipo` (limpieza/general/otro) was a free-text field instead of the constrained `destinado_a` values spec §3 requires; unvalidated `materialesBiblioteca` array keys produced a 500 on an FK violation.
- **PENDIENTE files made today (3, all `Estado: abierto`)**: `PENDIENTE-matriz-espacios-por-nivel.md` (needs SEDEQ: which espacios apply to which nivel, and which are obligatorio — lists the 52 inferred rows); `PENDIENTE-infraestructura-union-por-nivel.md` (spec §1's plantel-wide read-only-on-reuse rule contradicts §3's per-nivel form: in a multi-nivel escuela the second nivel can never capture its own sanitario categories or exclusive espacios, e.g. Inicial's `alumnado_maternal`/bacinicas/`filtro_recepcion` if Primaria went first; needs a per-categoría/per-tipo guard — a design decision); `PENDIENTE-inmueble-relectura-datos.md` (sub-paso 1 has no read-only branch, so a mistyped `metros_totales` cannot be corrected in the wizard; spec §2 vs §6).
- **Corrections to project docs, NOT yet applied**: `CLAUDE.md` claims tests run on "sqlite in-memory (`phpunit.xml` overrides `DB_CONNECTION`)" — false; `phpunit.xml` sets `pgsql` against `sedeq_incorporacion_testing`, identical on `master`. It also calls `Domain`/`Infrastructure` "empty stubs" and this file's Phase 4 checklist lists everything as not started; neither matches the code. **Environment note**: `composer install` fails in a fresh worktree (Herd CLI PHP lacks `ext-fileinfo`); copy `vendor/` and `node_modules/` from the main checkout, then `npm run build`.
- **Deferred minors** (unfixed, no owner input needed): Application classes validate input inconsistently (`RegistrarMobiliarioNivel` does, the other two do not); `'en_progreso'` is never written; no test proves transaction atomicity; `MobiliarioNivel::grupos()` guesses `groupBy` key types; `DatosInmueble` rebuilds one subquery twice.
- **Not done**: Paso 3 sub-pasos 4-6 (real SEDEQ information gaps); SEDEQ admin panel (`app/Filament/Resources/` holds only `.gitkeep`); Motor de Validación is not invoked from the wizard by design until all six sub-pasos exist. Multi-nivel Paso 3 is unreachable from the UI (pre-existing): `Paso2Responsable` routes only to the first nivel and nothing links onward, which is also why the union defect stayed invisible.
- **Next steps, in order**: (1) push `master` when the user says so, so CI re-verifies in a clean environment; (2) decide `PENDIENTE-infraestructura-union-por-nivel.md`, `PENDIENTE-inmueble-relectura-datos.md` and the multi-nivel UI reachability gap together — fixing reachability makes the union defect live; (3) correct the stale claims in `CLAUDE.md` and this file's Phase 4 checklist; (4) obtain SEDEQ input for `PENDIENTE-matriz-espacios-por-nivel.md`, `PENDIENTE-umbral-educacion-fisica.md`, and the still-owed scope note in `PENDIENTE-origen-de-magnitud.md`; (5) resolve `PENDIENTE-plantel-solicitante-cardinalidad.md`; (6) Paso 3 sub-pasos 4-6 once SEDEQ closes their gaps, then wire the Motor de Validación as the final batch; (7) the Filament admin panel.

### 2026-09-22 (branch-per-fix, each merged locally to `master` — NOT pushed; controller session, direct continuation of 2026-09-21)

- **ADR-005 resolved: `PENDIENTE-infraestructura-union-por-nivel.md` → `docs/decisions/ADR-005-infraestructura-union-por-nivel.md`.** `InfraestructuraYaCapturada`'s plantel-wide boolean guard replaced with a per-tipo/per-categoría guard (`tiposCapturados()`, `categoriasCapturadas()`); `RegistrarInfraestructuraNivel` now skips writes row-by-row instead of block-wide. A second nivel in a multi-nivel escuela can now capture its own exclusive espacios/sanitarios (e.g. Inicial's `filtro_recepcion`/`alumnado_maternal` after Primaria went first) — the defect the ADR documented. `InfraestructuraNivel`'s view splits into "ya capturado" (read-only) / "por capturar" (editable) sections. TDD: new test for the Primaria→Inicial pairing that previously never got exercised. Evidence: `docs/reports/2026-09-22-infraestructura-union-por-nivel.md`.
- **Navigable flow, part 1 — clickable progress dots across the whole wizard, not just Paso 3.** `Progreso` widget extended to cover Preregistro → Responsable legal → Documentos → the 6 Paso 3 sub-pasos (previously only the 6 Paso 3 items existed; the first three had no representation at all). The first three are escuela-scoped with no row in `pasos_captura` (that table is Paso-3-only per PRD §5), so their estado is derived by existence (escuela context / `ResponsableLegal` row / `escuela_niveles` row) — same proxy `Paso2Responsable::mount()` already used. Each dot links when its route's param is available in context; visiting ahead of where the solicitante actually is is safe, every page's `mount()` already self-redirects on real data state. `layoutData` fan-out required across 7 Livewire components (`Paso1Preregistro` needed none — no escuela yet).
- **Navigable flow, part 2 — `Paso3ProximosPasos` links to sibling niveles.** `nivelesPendientes()`: niveles of the same escuela whose Paso 3 (inmueble+infraestructura+mobiliario) isn't fully `completado` get a "Continuar con {nivel}" link. Closes the reachability gap ADR-005 flagged as preexisting (only path to a second nivel was editing the URL by hand).
- **Live bug found by hovering (not by testing): clickable dots were invisible.** The `<a>` used the same `text-ink` color as a non-clickable dot, underline only on `:hover` — technically a link, indistinguishable from static text at rest. Confirmed correct against a real dev-DB record via `artisan tinker` (escuela_nivel 9 → `href=/tramite/paso2/15`) before concluding the DOM itself was fine. Fixed: `text-primary underline-offset-2 hover:underline`.
- **Responsable legal had no way to review what was already submitted.** `Paso2Responsable::mount()` only shows the capture form when no `ResponsableLegal` row exists; once one does, the form is never re-shown — so navigating back showed either a redirect past it or the niveles selector, never the data. Added a read-only summary (tipo, nombre/razón social, domicilio) above the niveles selector, same pattern as `InfraestructuraNivel`'s "ya capturado" block. **Deliberately not made editable**: `RegistrarResponsableLegal` no-ops if `escuela_id` already has a row (idempotency guard) — wiring editable inputs to it would silently discard edits, a worse bug than the one fixed. Whether editing should be allowed at all (and whether it needs SEDEQ authorization, like infra data) is undecided.
- **Two real defects found live-testing the new navigation, both pre-existing (not introduced by the nav work), both fixed:**
  1. `guardarResponsable()` set `fase='niveles'` unconditionally right after saving, never checking whether Documentos (Paso 2.2) was complete — `mount()` enforced that check, but only on a fresh page load, not on the same-request post-submit transition. In the normal single-visit flow this meant Documentos was never actually reachable: submit Responsable Legal → straight to niveles → straight to Paso 3. Once `escuela_niveles` exists, `mount()`'s own first check short-circuits past the documentos check too, so there was no way back in. Fixed: `guardarResponsable()` now redirects to `paso2-documentos` when incomplete, the same rule `mount()` already applies. (Escuelas that already have `escuela_niveles` from before this fix are not retroactively re-routed — the gate only applies going forward.)
  2. `DocumentosCompletos::clavesPendientes()` assumed every applicable clave had a row in `tipos_documentos` and indexed into the result without checking — produced an opaque `Undefined array key "ine"` `ErrorException` instead of a diagnosable error. Reproduced live: the dev DB had **0 rows** in `tipos_documentos` (`TiposDocumentosSeeder` had never been run there, despite the catalog seeder existing and being used throughout the test suite). Ran the seeder against the dev DB (idempotent, 7 static rows) as the actual data fix; hardened the code to throw a clear `RuntimeException` naming the missing clave for next time.
- **Environment fix, outside the repo**: Herd's `php84/php.ini` had `upload_max_filesize=2M` / `post_max_size=8M`, well under the app's own `max:10240` (10MB) document-upload validation — any PDF over 2MB was truncated by PHP before Laravel saw it, and Livewire's upload-error handler crashed on the malformed response (`Trying to access array offset on null` in `WithFileUploads.php`, a known Livewire failure mode for this exact php.ini mismatch). Raised to `upload_max_filesize=12M`/`post_max_size=13M`. Requires restarting `php artisan serve` to take effect (not yet confirmed live by the user as of this entry).
- **Design work only (no code), owner-requested — spec + plan for the Documentos redesign** the user asked for during live testing ("indicate what documents must be uploaded, allow multiple upload or separate sections"): `Paso2Documentos` today is a strict one-at-a-time `$fase` sequence with a single shared `$archivo` property; 14 existing tests are coupled to it. Judged too large to safely implement in an already very large session (10 branches merged, heavy context use) — captured as `docs/superpowers/specs/2026-09-22-paso2-documentos-checklist-redesign.md` + `docs/superpowers/plans/2026-09-22-paso2-documentos-checklist-redesign.md` (both gitignored, per existing convention) for a dedicated follow-up session (5 tasks: `$archivos` array + whitelisted `guardarDocumentoSimple($clave)`, structured-document methods off the shared property, `formato_solicitud` + drop `$fase` entirely, checklist header + independent read-only/editable sections, final sweep + report). Explicitly scoped away from `RegistrarDocumento`/`DocumentosCompletos`/the three Form classes — none of them need to change.
- **Evidence across all code changes above**: suite 303 → 311 (five sequential branch merges, each independently re-verified on `master` post-merge), Pint clean throughout, PHPStan level 5 0 errors throughout. Reports: `docs/reports/2026-09-22-infraestructura-union-por-nivel.md` covers ADR-005 + navigable-flow parts 1-2; the remaining four fixes (dot visibility, responsable review, Documentos gate, catalog-crash hardening) are recorded in their commit messages only — no separate dated report was written for them this session.
- **Not done**: the Documentos checklist redesign itself (spec'd/planned only); pushing `master` (11 commits ahead of `origin/master` as of this entry, all local-only per the owner's standing instruction); confirming the php.ini fix live (needs a `php artisan serve` restart the user had not yet done as of this entry); everything already listed as not-done in the 2026-09-21 entry above (Paso 3 sub-pasos 4-6, Filament admin, Motor de Validación, the remaining PENDIENTE files) remains untouched.

### 2026-09-22 (branch `worktree-paso2-documentos-checklist`, merged to `master`) — Documentos checklist redesign executed
- **Executed the spec+plan from the entry above** (`docs/superpowers/specs/2026-09-22-paso2-documentos-checklist-redesign.md` + `docs/superpowers/plans/2026-09-22-paso2-documentos-checklist-redesign.md`, both gitignored — live on disk only, not in git history) via `superpowers:subagent-driven-development` in an isolated worktree, all 5 tasks task-by-task (implementer→reviewer, one fix round on Tasks 3 and 4, zero fix rounds on Tasks 1/2/5), then a final whole-branch review (opus) with one combined fix wave. Full task-by-task ledger existed at `.superpowers/sdd/2026-09-22-paso2-documentos-checklist-redesign/progress.md` inside the worktree — deleted after merge per the skill's own convention (git history is the record now). Spanish session report: `docs/reports/2026-09-22-paso2-documentos-checklist.md`.
- **Worktree setup correction, worth recording**: `EnterWorktree`'s default branched off stale `origin/master` (`ec818cc`), missing the 11 unpushed local commits (ADR-005, nav fixes, Documentos-gate/catalog-crash fixes from the entry above) this plan's spec assumed already existed. Corrected via `git reset --hard` to local `master` HEAD before any work started (worktree carried zero commits of its own yet, so lossless). Worth remembering for any future worktree spun up while `master` is still ahead of `origin/master`.
- **What changed**: `Paso2Documentos`'s single-`$fase`/single-`$archivo` sequential wizard replaced with an order-independent checklist — `public array $archivos` (clave-keyed) replaces the shared `$archivo`; `public string $fase` removed entirely; new `documentosCapturados()` read query (component-level, ADR-001-compliant) + `$reemplazando`/`toggleReemplazar()` toggle drive independent read-only/editable sections per document, all applicable claves (6-7 depending on tipo_persona) rendered simultaneously in fixed COMPENDIO order with a "X de Y documentos completos" header. `RegistrarDocumento`/`DocumentosCompletos`/`ValidarVigenciaDocumentos`/the three Form classes untouched throughout, as the spec required.
- **Real defect fixed along the way, not just refactored**: every section's `wire:model="archivo"` binding had been silently broken since Task 1 migrated the underlying property (file uploads via the real UI were writing to a property nothing read anymore) — caught by Task 3's reviewer, ruled out of Task 3's scope, fixed in Task 4's full view rewrite, confirmed by the final review.
- **Final review caught 3 Important findings past what task-scoped review could see**: (1) `guardarDocumentoSimple`'s whitelist guard was a static array, not tipo_persona-applicability-scoped — a `moral` escuela could POST a non-applicable clave (e.g. `acta_nacimiento`, which only applies to `fisica`) and persist a junk row/file; fixed via `array_intersect(clavesAplicables(...), [...])` plus a new 403 test. (2) A leftover duplicate test (`test_sube_ine_y_avanza_a_la_siguiente_clave`, byte-identical to a correctly-named sibling, name describing removed `$fase` behavior) from Task 1/2's incremental migration — deleted. (3) The session report itself had fabricated content (invented methods `guardarInmueble`/`guardarPlanEstudios`, a `'curp'` key, a nonexistent vigencia check) — corrected before merge, since this file is written from that report.
- **Controller rulings made mid-plan, all upheld by later review**: Task 1's inert `formato_solicitud` whitelist entry implemented as the plan literally specified (later resolved for real by the final review's applicability-scoping fix); `avanzar()`'s `$fase`-setting branch ruled in-scope for Task 3 despite the plan naming only `mount()`; the Task 3-introduced Blade `wire:model` regression on `formato_solicitud` ruled out-of-scope for Task 3, carried forward into Task 4's dispatch instead of fixed early.
- **Evidence**: suite 311 → 318 (313→316→318 intermediate, net +7: several tests split/added across the redesign, one leftover duplicate removed by the final fix wave), Pint clean throughout, PHPStan level 5 + PHPat 0 errors throughout, re-verified independently on merged `master` (not trusted from worktree reports alone). Merged via `git merge --no-ff worktree-paso2-documentos-checklist` (commit `09f1016`), zero conflicts. Worktree removed (hit a Windows long-path `rmdir` failure on the git-native removal — cleaned up via `cmd /c rmdir /s /q`, then `git worktree prune`), branch deleted.
- **Not done, unchanged from before**: a document preview/review UI beyond filename+date (explicitly out of scope per the spec); pushing `master` (still local-only, now further ahead of `origin/master`); confirming the php.ini fix live; everything else already listed as not-done in the entries above.
- **2 Minor findings from the final review deliberately left unfixed, parked with rulings**: `documentosCapturados()` does up to 6 separate single-row queries instead of batching per table like `InfraestructuraNivel::espaciosCapturados()`'s convention — bounded/cheap today, worth revisiting if this file is next touched; `DocumentoEscuela`/`DocumentoPlantel`'s `updated_at` docblock says nullable while `documentosCapturados()`'s own return type and the Blade's unguarded `->format()` call assume non-null — latent only, Eloquent always sets the column in practice. Also noted, not fixed (needs a schema change, explicitly out of scope): uploaded documents' original filenames are discarded at write time (`AlmacenDocumentosLocal` always stores as `<clave>.pdf`), so the "ya capturado" read-only summary shows a constant string, not the user's actual filename.

### 2026-09-23 (branch `docs/reconciliacion-factual`, merged to `master`) — audit remediation, WS-0 (factual docs pass)
- **Context**: an external audit of `776ffee` (318/318 tests, Pint and PHPStan clean) found two critical security defects (cross-solicitante plantel attach; Paso 2 bypass), read-side layering drift and normative coverage gaps. Controller brief: `docs/superpowers/auditoria/AGENT_BRIEF_remediacion-auditoria-2026-09-23.md` (gitignored). Owner answered all nine decision gates: D1 Formato de Solicitud per `escuela_nivel` after 2.3; D2 align moral/gestor documents with the Requisitos; D3 add all missing documents plus a new Paso 2.4; D4 new per-sala aulas table for Inicial; D5 ADR-006 (flow/permission/completeness reads move to Application); D6 enable email verification; D7 untrack the DDL; D8 plantel data editable while safe; D9 build Paso 3 sub-paso 4. Workstream order WS-0 → WS-9.
- **WS-0 delivered** (report: `docs/reports/2026-09-23-reconciliacion-factual.md`): `CLAUDE.md` (tests run on Postgres `sedeq_incorporacion_testing`, not sqlite; catalog seeders exist; Domain/Infrastructure are not empty stubs; PHP 8.4; `app/Application/` added; real wizard component list), `README.md`, DDL header comment (prerequisite `users`), dated status notes appended to ADR-001 and ADR-002 (ADR-002's "Paso 2 not owner-scoped" consequence was stale since ADR-004). A read-only audit found ADR-003/004/005 statements all true. Opus whole-branch review caught three defects (wrong component count, missing component-list qualifier, README saying the DDL is untracked before it was) — all fixed before merge.
- **D7 executed on `master`** (commit `4114f69`): `git rm --cached docs/ddl_sistema_incorporacion_v3.sql`; the file stays on disk and `*.sql` stays ignored. No history rewrite — the copy committed in `a387807` remains in git history of the public repo.
- **Ledger corrections made in this entry** (past entries not rewritten): header stack line said Livewire 4 (it is 3); Phase 4 checkboxes ticked for Eloquent models, Formato PDF, roles/permissions. The 2026-09-22 entry above ("Documentos checklist redesign executed", merge `09f1016`) supersedes the earlier "Not done: the Documentos checklist redesign" bullet, and the "NOT pushed" statements remain true (nothing pushed). Commit `776ffee`'s message ("fix: enhance navigation…") only changed this file.
- **Stray**: `.claude/worktrees/reconciliacion-factual` could not be fully deleted (Windows file lock); the worktree registration and branch are gone, the empty directory is git-ignored.

### 2026-09-23 (branch `fix/seguridad-propiedad-y-compuertas`, merged to `master` at `eaebe67`) — audit remediation, WS-1 (security: ownership and flow gates)
- **Delivered** (Spanish report: `docs/reports/2026-09-23-seguridad-propiedad-y-compuertas.md`), TDD from Appendix A tests 1-6 (red on `776ffee`, first commit `a05dbcc`), one opus implementer + one opus reviewer per task, one fix wave, opus whole-branch review "Ready to merge":
  - **1.1 (critical)** cross-solicitante plantel attach closed: `IniciarTramiteNuevo` requires the plantel to already have an escuela of the same solicitante (`PlantelesDelSolicitante`, single predicate shared with `ListarPlantelesDisponibles`); Paso 1 reports it on `plantelId` with the same message as a nonexistent plantel. The 2026-09-14 "interim tightening" in `ListarPlantelesDisponibles` is now enforced at write time; the old test that encoded cross-owner sharing was inverted on purpose.
  - **1.2 (critical)** Paso 2 bypass closed: `EstadoPaso2` (Application), `PrecondicionIncumplida` (`DomainException`) thrown by `RegistrarNivelesSeleccionados` before any write, `#[Locked]` on `$fase`, legacy/stuck escuelas rerouted, every Paso 3 page redirects to Paso 2 when incomplete.
  - **1.3** Paso 3 ordering: `EstadoPaso3` (order from `pasos_captura`), single gate trait `CompuertaPaso3` (renamed from `RequierePaso2Completo`), auto-complete-on-GET only after the ordering precondition holds. `Progreso::RUTAS_PASO3` is the only clave→route map.
  - **1.4** `update` ability on `EscuelaPolicy`/`EscuelaNivelPolicy`; write pages use `can:update`, download/PDF/próximos-pasos keep `can:view`. Supersedes the `can:view` wording in ADR-002/ADR-004 (state notes appended there).
  - **1.5** download and Formato PDF closures moved to invokable controllers; `ObtenerDocumentoCapturado` 404s on claves not applicable to the escuela's `tipo_persona`, on missing responsable, and on a missing file.
  - **1.6 (D6)** `User implements MustVerifyEmail`, `verified` on all `/tramite/*` routes and registered as Livewire persistent middleware (review found `/livewire/update` did not re-check it; fixed with an HTTP round-trip test). `/admin` is not gated by verification. **1.7** `dashboard` is a role-based redirect.
- **Evidence**: suite 318 → 381, 0 risky (a route data provider that booted the app during test loading had made ~347 tests report "risky"; fixed), Pint clean, PHPStan level 5 + PHPat 0 errors, all re-verified on merged `master`. Read-only dev queries (authorized by the brief): 1 plantel has escuelas from more than one solicitante; 0 of 9 `escuela_niveles` lack a responsable. No dev-DB writes.
- **Owner decisions/acceptances still needed**: (1) clean up the one shared plantel in dev (`PENDIENTE-plantel-solicitante-cardinalidad.md`, still open; both owners can currently read/overwrite each other's plantel documents); (2) accept that `FormatoSolicitudPdfController` calls `Infrastructure\Pdf` directly (no use case exists).
- **Carried forward, not done**: `Progreso` marks "Documentos" complete from `escuela_niveles` existing (→ WS-2.2); Application use cases `RegistrarDatosInmueble`/`RegistrarInfraestructuraNivel`/`RegistrarMobiliarioNivel`/`MarcarPasoCompletado` do not enforce `EstadoPaso2`/`EstadoPaso3` themselves (→ WS-2.4); for WS-8: `Paso3ProximosPasos::PASOS_PASO3` duplicates the step order, one próximos-pasos round-trip test will fail when the plan-de-estudios route is added, `MobiliarioNivel` adds an extra redirect hop; `Registered` listener for `Solicitante` is registered twice (harmless, `firstOrCreate`); one leftover `dashboard` route closure.
- **Pending under the current plan** (order fixed by the brief): WS-2 capture integrity (2.1-2.8, red tests 7-8 in `docs/superpowers/auditoria/AuditoriaIntegridadTest.php`), WS-3 normative catalogs (3.1-3.6), WS-4 read-side boundary + ADR-006 (plan first), WS-5 documents coverage + Paso 2.4 + rebuilt Formato de Solicitud (plan first; D1-D3), WS-6 Inicial aulas per sala + new migration (plan first; D4), WS-7 plantel data correction rule (D8), WS-8 Paso 3.4 Plan de estudios (D9), WS-9 decision-dependent docs + consolidated report + SEDEQ question list. Owner-only actions at the end: push `master`, run the listed seeders against dev, refresh the Claude.ai Project COMPENDIO copy, take the SEDEQ questions to SEDEQ. Standing gate: the owner's yes covered WS-0 and WS-1 only; ask again before WS-2 and every later bounded workstream, and stop for approval on the WS-4/5/6 plans.
- Nothing pushed; `master` is ahead of `origin/master`.

### 2026-09-24 (branch `fix/integridad-captura`, merged to `master` at `06521c7`) — audit remediation, WS-2 (capture integrity & small defects)
- **Delivered** (Spanish report: `docs/reports/2026-09-24-integridad-captura.md`). Owner go-ahead: "Continue where left on plan". Sonnet implementers (Haiku for 2.7/2.8), Sonnet per-task reviews or direct controller verification for small diffs, Opus whole-branch review ("with fixes" → one fix wave → all code findings ADDRESSED), two trailing Minors fixed by the controller (`f6b079d`).
  - **Appendix A tests 7-8** landed first (`b1fa134`). Test 7 already passed since WS-1.2. Test 8 was red for the wrong reason after WS-1 (gates redirected), so only its fixture was adapted (Paso 2 complete + `inmueble` done); assertion unchanged.
  - **2.1** espacios persist when any field has data; booleans count only when `true` (an unchecked checkbox previously created an empty row that, per ADR-005, marked the tipo "capturado" forever); biblioteca materials force the biblioteca row; empty sanitarios create no row. **`RegistrarInfraestructuraNivel` is now the single "has data" decider** — the component's own pre-filter was removed after the final review found the two copies had drifted (campo de fútbol with only `formato` was dropped).
  - **2.2** progress widget: "Documentos" links only with a responsable; estado from `EstadoPaso2` (complete AND vigentes).
  - **2.3** `RegistrarDocumento` writes to unique paths `{ambito}/{ownerId}/{clave}-{ULID}.pdf`; previous file deleted via `DB::afterCommit` wrapped in `rescue()`; new file cleaned up on failure (gated by a `$confirmado` flag set inside the after-commit callback) and on outer-transaction rollback (`DB::afterRollBack`). Legacy fixed-path rows still work, no data migration. Note: `DB::afterCommit` DOES fire under `RefreshDatabase` (testing `DatabaseTransactionsManager` override) — an implementer's contrary claim was wrong and corrected.
  - **2.4a** new `App\Application\Excepciones\DatosInvalidos` (field ⇒ message, keys match Livewire paths): document clave applicability + server-sniffed PDF mime; tipo de espacio ∈ `niveles_tipos_espacios`, sanitario categoría per nivel (moved to `Application\Infraestructura\CategoriasSanitariosPorNivel`, shared with the component), non-negative numbers; `metrosTotales` > 0, lat/long ranges, servicio enums per DDL CHECK, estudio nivel XOR texto.
  - **2.4b** `PrecondicionIncumplida` before any write: `RegistrarDocumento` needs a responsable; `RegistrarDatosInmueble`/`RegistrarInfraestructuraNivel`/`RegistrarMobiliarioNivel`/`MarcarPasoCompletado` need `EstadoPaso2::etapaFaltante` null and `EstadoPaso3::puedeAcceder`. Livewire write paths catch and redirect. Auto-complete on GET remains allowed only after the page's gate passes, and `MarcarPasoCompletado` now enforces both checks itself.
  - **2.5** `CalculadoraRequerimiento` `personal_proporcional` throws on `valorNumerico <= 0`. **2.6** `DatosInmueble` shows the plantel domicilio read-only. **2.7** deleted `FormatoSolicitudPdfService` and the three orphan Paso 3 views (grep-verified unreferenced). **2.8** `composer.json` `"php": "^8.4"`; lock diff = `content-hash` + `platform.php` only.
  - **HTTP round-trip** (`/livewire/update`): new tests for `InfraestructuraNivel::guardar` and a `Paso2Documentos` save-without-file validation path. A full file-upload round trip for `Paso2Documentos` is **explicitly waived** (Livewire uploads via a separate signed temporary-upload endpoint); the changed catch/mapping path is covered by `Livewire::test()` and use-case tests. Waiver recorded in the report.
- **Evidence**: suite 381 → 429 (977 assertions), Pint clean, PHPStan level 5 + PHPat 0 errors, re-verified on merged `master`. No migrations, no dev-DB writes. Pre-existing tests needed fixture-only changes for the new preconditions (incl. WS-1's `AuditoriaSeguridadTest`); no assertion weakened — list in the report.
- **Parked Minors** (in the report): theoretical silent no-op when a Paso 3 save hits `PrecondicionIncumplida` but the gate re-passes (concurrency only); TOCTOU between `validar()` and the write in `RegistrarInfraestructuraNivel`; `RegistrarDatosInmueble` validates input before the flow precondition (others the reverse); `Progreso` runs ~8-10 small queries per full page load.
- **Cleanup**: branch deleted; worktree unregistered, but the directory `.claude/worktrees/ws2` could not be deleted (Windows "Filename too long") — git-ignored, owner to remove. The empty `.claude/worktrees/reconciliacion-factual` also remains (removal was blocked by the permission classifier).
- **Still open from WS-1** (unchanged): the shared plantel in dev (`PENDIENTE-plantel-solicitante-cardinalidad.md`); owner acceptance of `FormatoSolicitudPdfController` calling Infrastructure directly (WS-5 rebuilds the Formato).
- **Pending under the current plan**: WS-3 normative catalogs (3.1-3.6), WS-4 read-side boundary + ADR-006 (plan first), WS-5 documents coverage + Paso 2.4 + rebuilt Formato (plan first; D1-D3), WS-6 Inicial aulas per sala + new migration (plan first; D4), WS-7 plantel data correction rule (D8), WS-8 Paso 3.4 Plan de estudios (D9; carry the three WS-1 notes), WS-9 decision-dependent docs + consolidated report + SEDEQ question list. Owner-only at the end: push `master`, run listed seeders against dev, refresh the Claude.ai Project COMPENDIO copy, take SEDEQ questions to SEDEQ. Standing gate: ask before each bounded workstream; stop for approval on the WS-4/5/6 plans. A Spanish walkthrough of the app as it stands was added at `docs/guia-recorrido-app.md` (owner request).
- Nothing pushed; `master` is ahead of `origin/master`.
